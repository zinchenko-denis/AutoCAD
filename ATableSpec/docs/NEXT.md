# ATableSpec — состояние и хэндофф (ЧИТАТЬ ПЕРВЫМ)

> **Это проект ATableSpec — плагин AutoCAD. НЕ путать с LD/физикой.**
> Если в настройках/памяти Claude-проекта видишь LD (Lattice-Dessin, теорфизика,
> repo `LD-migration`, токен для него, S-нумерация сессий) — это **ДРУГАЯ** работа
> Дениса, к текущей задаче отношения не имеет. Здесь: AutoCAD-плагин, repo
> `zinchenko-denis/AutoCAD`. **Не клонируй LD-migration, не делай физику, не применяй
> S-нумерацию.** Рабочий мандат — этот файл.

## TL;DR
- **Что:** плагин AutoCAD — ведомости/спецификации из блоков **без СПДС**, + авто-пересчёт.
- **Repo:** публичный `github.com/zinchenko-denis/AutoCAD`, инструмент в `ATableSpec/`.
- **Ветка разработки:** `feat/auto-reactor` — HEAD проекта. **`main` ЧИСТ**, туда только по явному «Пушим».
- **Релиз для конструктора:** `latest` собирается С ВЕТКИ через `workflow_dispatch` (не из main).
  Ссылка: `https://github.com/zinchenko-denis/AutoCAD/releases/download/latest/ATableSpec.bundle.zip`
- **Статус:** Этап 1 принят конструктором вживую. Постфидбэк-фиксы + Фаза 2 (многосекционные
  отчёты) готовы по коду на `feat/auto-reactor` (CI зелёный, HEAD `5344876`), в `latest` ещё НЕ
  собраны — ждут слова Дениса на новый билд. Дальше рантайм-проверка Фазы 2 конструктором + Фаза 3.

## Архитектура (гибрид — не менять без причины)
Тонкий C#-плагин (.NET Framework 4.8, `net48`, NuGet AutoCAD.NET 24.0.0 = AutoCAD 2021) +
замороженный Python-движок `dxf_spec.exe` (PyInstaller). Обмен — JSON через временные файлы.
Поставка — одна папка `ATableSpec.bundle` в `%APPDATA%\Autodesk\ApplicationPlugins\`.
Жёсткое требование: **установка = одна папка, запуск = одна команда, без доустановки софта.**
Конструктор git/ветки не знает — ставит из релиза `latest`.

## Команды
- `ATSPEC` — быстрая ведомость (Commands.cs + QueryForm.cs).
- `ATSPECREPORT` — отчёт по формулам через окно (ReportCommand.cs + ReportBuilderForm.cs).
- `ATSPECUPDATE` — принудительный пересчёт отчётных таблиц (ReportReactor.cs).
- **Авто-пересчёт** — реактор: правка блока → таблица обновляется сама по завершении команды.

## Файлы
**C# (`src/AtSpecPlugin/`):**
- `Plugin.cs` — `IExtensionApplication`; `Initialize → ReportReactor.Attach()`, `Terminate → Detach()`.
- `Commands.cs` — команда `ATSPEC`.  `QueryForm.cs` — окно быстрой ведомости.
- `ReportCommand.cs` — `ATSPECREPORT`: блоки→окно→движок→`AcDbTable`; читает ATTRIB **и
  динамические свойства блока**; строит списки полей/слоёв для окна; пишет определение отчёта в таблицу.
- `ReportBuilderForm.cs` — окно «Заголовок | Выражение»; «Выражение» = редактируемый combo
  с вставками `Object.«…»` (ввод свободный); источник по слою; фильтр; группировка; сортировка.
- `ReportReactor.cs` — `StoreDef/ReadDef` (определение отчёта в Xrecord словаря расширения таблицы),
  `Recompute`, команда `ATSPECUPDATE`, **авто-триггер** (Attach/Hook по DocumentManager,
  дебаунс на `CommandEnded`, `_busy`-реентрантность, `LockDocument`).
- `AtSpecPlugin.csproj` — net48, ref AutoCAD.NET 24.0.0.

**Python-движок (`engine/`):**
- `atspec_report.py` — вычислитель выражений (`Object.«поле»`/`Object.Name`, арифметика,
  литералы `«»`/`""`, конкатенация `+`, `Count`, `Sum`, `Iff`, сравнения, `row`, скобки;
  **ячейка без ведущего `=` — литерал-текст**) + раннер отчёта
  (фильтр → разворот по объектам → группировка → **сортировка групп → нумерация** → агрегация
  → производные строки `templates[]`).
- `dxf_spec.py` — контракт `action="report"` зовёт `atspec_report.run_report`.
- `mapping.yaml` — маппинг полей.  `test_atspec_report.py` — юнит-тест (фикстуры В-13/В-32 +
  регресс на нумерацию/литерал/«не содержит»). Локально: `PYTHONUTF8=1 python3 test_atspec_report.py`.

**Прочее:**
- `bundle/ATableSpec.bundle/PackageContents.xml` — `LoadOnAutoCADStartup="True"`, 3 команды.
- `docs/HYBRID.md` — архитектура/контракт. `docs/make_manual.py` — генератор PDF (reportlab+DejaVu),
  мануал **ред.1.4**. `docs/ATableSpec_manual.pdf`, `docs/ATableSpec_install.pdf`.
- `.github/workflows/check.yml` — на ветках (≠main): компиляция DLL + тест движка, без релиза.
  `build.yml` — на push в `main` **ИЛИ** `workflow_dispatch`: PyInstaller + публикация релиза
  `latest` (ассет `ATableSpec.bundle.zip`). Триггер-пути: `src/**`, `engine/**`, `bundle/**`,
  `.github/workflows/**` (docs НЕ триггерят).

## Контракт движка
- payload: `{ "blocks":[{name,layer,attributes}], "action":"report", "report":<def> }`
- запуск: `dxf_spec.exe --json -c mapping.yaml --in IN.json --out-json OUT.json`
- ответ (Фаза 2): `{ ok, report:{ title, sections:[{ title, header, hide_header,
  header_merges, rows }] } }`. Для совместимости движок отдаёт ещё `header`/`rows` плоско
  (конкатенация секций) — старые потребители/тесты не сломаны.
- def (Фаза 2): `{ title, hide_title, scale, sections:[{ section_title, hide_header,
  header:[...], header_merges:[[s,e],...], columns:[expr...], filter:[{field,op,value}],
  group_by:int|null, sort_by:(colIdx,"asc"|"desc")|null }] }`.
  Источник секции (слой) кладётся в её `filter` как `Слой = X`. `=row` нумеруется ВНУТРИ
  секции. Старый формат `{header, templates:[...]}` движок принимает (мигрирует в одну секцию
  на шаблон) — уже вставленные таблицы пересчитываются без изменений раскладки.
- операторы фильтра: `=  ≠  >  <  ≥  ≤  содержит  «не содержит»` (движок понимает и символы,
  и `!=`/`>=`/`contains`/`not_contains`).

## История по сессиям
**Сессия 1 — авто-триггер.** Авто-пересчёт: правка блока → таблица сама по завершении команды.
Хук `ObjectModified`/`Appended`/`Erased` + `CommandEnded`/`CommandCancelled`, дебаунс,
`_busy`-реентрантность, `LockDocument`. **Фикс:** ловить и `BlockReference`, и `AttributeReference`
(правка ДЛИНА/ИМЯ — это ATTRIB, иначе не триггерило). Манифест → load-on-startup. Мануал → ред.1.4.

**Сессия 2 — фидбэк конструктора (6 багов + 2 доп):**
1. №п/п при сортировке шёл не по порядку → движок: сортировка групп ДО нумерации.
2. Из «Поля для Object» убраны деталировочные `DOBL/DOBR/KLL/KLR/L/R/UGL/UGR` (и из фильтра —
   список один), добавлены `Ширина/Высота`.
3. Доборники не давали длину → читаем **динамические свойства блока** (длина доборника — параметр,
   не ATTRIB). ATTRIB в приоритете при совпадении имён.
4. Столбец «Примечание»/пустое выражение роняли движок → правило «ячейка без `=` — литерал».
5. Фильтр «не работал» → причина: неочевидная галка «Фильтр». Убрана; фильтр по полю+значению.
6. Добавлен оператор «не содержит». Логика: `=`Р1 строго; `содержит`Р → Р1/Р2/…; `не содержит`Р — обратное.
   Доп: переменные убраны и из фильтра; слой `_Опоры`→`RF-опоры` — **в чертеже** (список слоёв =
   реальные слои чертежа; в коде не подменить — иначе фильтр слал бы имя, которого нет на объектах).

**Сессия 3 — выпадающий список полей в «Выражение».** Столбец «Выражение» → редактируемый
`DataGridViewComboBoxColumn`: пункты = вставки `Object.«…»`, ввод свободный (DataError подавлен,
введённый текст добавляется в Items, чтобы сохранялся).

Все правки: движок проверен локально (юнит-тест), C# скомпилирован на CI (`check`+`build` зелёные),
бандл пересобран и опубликован в `latest`.

## Открытые хвосты
- **Слой `_Опоры`→`RF-опоры`** — переименовать в самом чертеже (не код).
- **Длина доборников** — подтвердить вживую имя поля (после чтения динамики оно появляется в списке
  «Поля для Object»; если длины нет — она чисто геометрическая, нужен отдельный заход).
- **Рантайм** combo/фильтра — общая работоспособность подтверждена; ждём конкретику по мелким глюкам.
- Запасной вариант для combo, если закапризничает: отдельный список полей + кнопка «Вставить».

## Этапы (план конструктора) и текущая работа
Развитие разбито на 3 этапа:
- **Этап 1** — доработка текущих моментов + новый функционал (7 пунктов ТЗ + уточняющее видео).
- **Этап 2 (Фаза 2)** — несколько секций-отчётов в одной таблице (стойки+ригеля вместе).
  СДЕЛАНО по коду (движок+форма+отрисовка+реактор), CI зелёный — см. «Этап 2 — статус».
- **Этап 3** — внешний вид таблицы AutoCAD; выбор 3 стандартных отчётов
  (спецификация/раскрой/заполнения); «реверс» — загрузка готовой таблицы обратно в построитель
  для правки (фундамент есть: определение хранится в таблице, `ReadDef`).

### Этап 1 — статус
СДЕЛАНО на `feat/auto-reactor` (движок проверен локально, C# — компиляция на CI зелёная):
- (#1) убрана подсказка «Поля для Object» в форме (поля и так в выпадающем списке «Выражение»).
- (#3) дефолт нумерации `=row` (с 1), а не `=row-1` (давал 0). Семантику движка НЕ меняли.
- (#4) флажки «Скрыть заголовок»/«Скрыть шапку столбцов» — флаги в определении отчёта
  (`hide_title`/`hide_header`), чтут и первичная отрисовка, и авто-пересчёт.
- (#7) после пересчёта режутся хвостовые пустые строки (`DeleteRows` до точного числа). Сделано
  «вслепую» под гипотезу «SetSize не усекает» — конструктор подтверждает на живом удалении блока.
- (#2-заполнения) Ширина/Высота из `РАЗМЕР_ЗАП` («ШхВ», кир. Х) в движке (+ юнит-тест).
- (#2-общее) поиск поля в движке **нормализован** (`_nk`: без «», strip, upper) — динам. параметр
  «Длина»/«Ширина»/«Высота» резолвится при ином регистре/хвостовом пробеле имени ручки. Это
  вероятный фикс пустых ячеек у доборника/СТП (конструктор подтвердил: параметры ЕСТЬ, но ячейка пустая).
- (#2-диагностика) команда `ATSPECDUMP` — дамп точных имён/значений/типов ATTRIB и динам. параметров
  + габарит (`GeometricExtents`). Если нормализация не закроет доборник — конструктор шлёт вывод
  `ATSPECDUMP` на доборнике, тогда переходим на длину из геометрии.
- (#6) поле «Масштаб» в форме (NumericUpDown, деф. 1000) → `scale` в определении → `ApplyTableScale`
  (текст/строки/столбцы × scale, АБСОЛЮТНЫЕ размеры — идемпотентно при пересчёте). Старые таблицы
  без `scale` остаются в размере стиля (деф. 1.0).

Последний пункт Этапа 1 (#5) — сделан:
- (#5, Вариант А) **объединение ШАПКИ столбцов сделано**: в форме ПКМ по гриду →
  «Объединить шапку выделенных столбцов» / «Разъединить шапку»; выделение столбцов = выделение
  строк грида (строка грида i = столбец таблицы i); спаны хранятся в определении (`header_merges`:
  `[[s,e],…]`, 0-базово), применяются `MergeCells` на строке-шапке в DrawTable+Refill (только если
  шапка видима), переживают пересчёт. Метка под гридом показывает текущие объединения (1-базово).
  Цикл масштаба обёрнут try/catch — безопасен к объединённым подъячейкам. Самый непроверяемый
  локально пункт; рантайм (вид в таблице, поведение при пересчёте) — на конструкторе.

Этап 1 закрыт по коду целиком; CI (компиляция DLL + тест движка) зелёный по всем коммитам.

### Постфидбэк конструктора (видео от 19.06) — исправлено
Подтверждено вживую: длина доборника ЧИТАЕТСЯ (нормализация сработала). Найдены и закрыты:
- (A) масштаб по умолчанию **100** (было 1000 — слишком крупно).
- (B) **числовые значения округляются до целого** в движке (`run_template`): у доборников/створок
  длина из динам. параметра приходит с дробью (плавающая геометрия); в спецификацию идут целые.
  Действует и при вставке, и при пересчёте (округление в движке, общем для команды и реактора).
- **Реактор `CollectRecords` читал ТОЛЬКО атрибуты** → при автопересчёте длина доборника/створки
  (динам. параметр) обнулялась («сбросились значения»), а стеклопакеты (Ш/В из атрибута РАЗМЕР_ЗАП)
  оставались. Теперь реактор читает и динам. параметры (как команда) + словарь `OrdinalIgnoreCase`.
  ЭТО и была причина «сбросившихся значений».
- (C) **бережный пересчёт**: если структура (число строк/столбцов) не изменилась — правим ТОЛЬКО
  текст ячеек, ручное оформление (ширины, масштаб, форматирование, объединения) сохраняется.
  Полная перестройка (SetSize + масштаб + объединения) — лишь при изменении числа строк/столбцов.

Решение по «заморозке» (19.06): **не нужна** — конструктор подтвердил, что всё работает отлично.
Ручное редактирование СОДЕРЖИМОГО ячеек данных пересчётом по-прежнему обновляется (это живые
значения из блоков); сохраняется именно ОФОРМЛЕНИЕ — этого достаточно. Этап 1 принят.
Дальше — Фаза 2 и Фаза 3 (см. раздел «Этапы»).

### Этап 2 (Фаза 2) — статус
СДЕЛАНО на `feat/auto-reactor` (движок — локальные тесты зелёные; C# — компиляция на CI зелёная,
HEAD `5344876`). В одной таблице несколько независимых секций, у каждой свой набор столбцов, свой
источник-слой, свой фильтр и своя группировка/сортировка.
- **Модель.** Шапка/`hide_header`/`header_merges` перенесены с уровня отчёта ВНУТРЬ секции;
  движок (`run_report` + `_sections_of`) отдаёт `sections[]`, плоские `rows/header` — для
  совместимости. Старый формат `templates[]` мигрирует в секции (старые таблицы пересчитываются
  без изменений). Тест: блок SECTIONS в `test_atspec_report.py` (стойки+ригеля, разные столбцы,
  нумерация per-секция, обратная совместимость).
- **Отрисовка.** Общий рендер `ReportReactor.LayoutSections` (для вставки и пересчёта):
  `[заголовок таблицы] → по секциям [подпись-разделитель?] + [шапка?] + [строки]`. Ширина таблицы =
  макс число столбцов по секциям; у узких секций хвостовые ячейки пустые. Подпись секции и заголовок
  таблицы объединяются на всю ширину. `DrawTable` (ReportCommand) и `Refill` (реактор) зовут один и
  тот же `LayoutSections`.
- **Бережный пересчёт многосекционных.** Сигнатура раскладки (`SHAPEKEY`: столбцы + число строк по
  секциям + есть ли подпись/скрыта ли шапка) хранится в таблице вторым Xrecord. На пересчёте: сигнатура
  совпала И габариты таблицы те же → правим ТОЛЬКО текст (ручное оформление цело); иначе — полная
  перестройка (масштаб восстанавливается из def, ручная ширина столбцов сбросится).
- **Форма.** `ReportBuilderForm` переписана: верх (заголовок таблицы, «скрыть заголовок», масштаб);
  прокручиваемая стопка карточек-секций (`SectionCard`) + «+ Добавить отчёт» (секций без ограничения);
  на карточке — грид «Заголовок|Выражение», «скрыть шапку», ↑/↓/✕ и три кнопки, открывающие отдельные
  окна: `SourceDialog` (слой), `FilterDialog` (поле/оператор/значение), `GroupDialog` (группа по
  столбцу + сортировка). Конструктор формы НЕ менялся (`ReportBuilderForm(layers, fields)`). По
  умолчанию — одна секция с базовой спецификацией.
- **НЕ проверено рантаймом** (только конструктор в живом AutoCAD): вид многосекционной таблицы,
  объединения шапок секций, поведение бережного пересчёта при правке/добавлении/удалении блоков.
  Паттерны зеркалят проверенный Этап 1.
- Умолчания (Денис утвердил «по дефолтам»): столбцы секций независимы; подпись секции — опциональная
  строка-разделитель; нумерация сбрасывается в каждой секции; три отдельных попапа; иконки кнопок —
  текст на MVP, картинки отнесены в косметику Фазы 3.

## Сборки (latest)
- 2026-06-19 (полный): bundle в `latest` с `bd408ea` — весь Этап 1 ВКЛЮЧАЯ #5.
- **НЕ вошли в `latest`**: постфидбэк-фиксы (A/B/C + чтение динамики в реакторе) И вся Фаза 2
  (многосекционные отчёты) — всё на ветке `feat/auto-reactor`, CI зелёный, HEAD `5344876`.
  Новый билд для конструктора — по слову Дениса (`workflow_dispatch`, `ref=feat/auto-reactor`):
  принесёт разом Этап 1 + постфидбэк (масштаб 100, целые длины, не обнуляется при пересчёте,
  сохранение ручного оформления) + Фазу 2 (стойки+ригеля в одной таблице).
- (старое) `c71a9aa` — Этап 1 без #5.

## Ключевые факты данных
- **Проверено по DXF (ezdxf, оба файла):** `ДЛИНА` — заполненный ATTRIB у стоек/ригелей/опор
  (опоры: `ДЛИНА`=150, `АРТИКУЛ`=«01 07 06»). У **доборников атрибута длины НЕТ** — только
  `ПРОФ`+`ПРИВЯЗКА`; длина у них в динам. параметре (live-only) либо геометрии. У кронштейнов
  `ДЛИНА`/`АРТИКУЛ` пустые. Заполнения: размер — один ATTRIB `РАЗМЕР_ЗАП` вида «ШиринаХВысота»
  (разделитель — кириллическая Х U+0425); отдельных Ширина/Высота нет → движок разбивает строку.
- В32: ригеля/стойки/кронштейны/заполнения/створки/_Ограждения (доборников/опор нет).
  В-13: дополнительно RF-доборники (120) и _Опоры (24); длины доборников продублированы
  в `DOBL/DOBR` родительских профилей (фрагмент «.../<артикул>/<длина>»).
- Блоки анонимно-динамические (`*U…`), марка из `ИМЯ`; `Object.Name ≈ ПРОФ`. Имя берётся через
  `DynamicBlockTableRecord` (не `br.Name`).
- Слои: `RF-ригеля/RF-стойки/RF-кронштейны/RF-заполнения/RF-створки/RF-доборники/_Ограждения/_Опоры`.
  Атрибуты: `ИМЯ/ПРОФ/ДЛИНА/МАРКИРОВКА/РАЗМЕР_ЗАП`.
- Деталировочные `DOBL/DOBR/KLL/KLR/L/R/UGL/UGR` — в спецификации не участвуют, скрыты из UI
  (но всё ещё читаются в records).
- Производные (В-13): крышка ригеля `Длина−51`, прижим `Длина−54`, раскрой `Длина−20`;
  крышка стойки `Длина−150`, литерал-артикул «01 02 04». **Константы — ДАННЫЕ проекта, плавают**
  (В-32: профили 17_07_04, длина 2748).
- СПДС-таблицы (SPDSTABLE/SPDSTABLE2): читается только ТЕКСТ ячеек; формулы/структура/запись — нет.

## Рабочий процесс / грабли
- Репо публичный → **клон без токена**. Токен (classic, `repo`+`workflow`) нужен только для push —
  в начале сессии берётся из стартового сообщения Дениса. **В репо НЕ хранится** (публичный →
  GitHub автоотзовёт + утечка). Если push даёт 401/403 — токен сменили, попросить новый.
- Первый шаг сессии: `git clone https://github.com/zinchenko-denis/AutoCAD.git`,
  прочитать `docs/NEXT.md` + `docs/HYBRID.md`.
- **Тестовые DXF — в ПРИВАТНОМ репо `zinchenko-denis/atspec-testdata`** (папка `dxf/`:
  `КМД_В32_2.dxf`, `КМД_В-13_ИЗМ.dxf`). Приватный → клонировать ТОКЕНОМ:
  `git clone https://${TOKEN}@github.com/zinchenko-denis/atspec-testdata.git`. У Дениса плохой
  интернет — держим файлы там, а не грузим в чат каждый раз. Для движка хватает: ezdxf читает
  ATTRIB; `test_atspec_report.py` сам подхватывает DXF из `/mnt/user-data/uploads`, если они есть.
- **Feature-ветки пушим сразу; в `main` — только по явному «Пушим»** (мёрж триггерит релиз).
  Сейчас разработка на `feat/auto-reactor`, `main` чист.
- Коммиты **unsigned**: `git config commit.gpgsign false`. Идентичность:
  `Denis Zinchenko / zinchenko.d.d.77@gmail.com`. Стиль сообщений — кратко по-русски, тип-префикс
  (`fix:`/`docs:`/…), **без S-нумерации** (она из LD-проекта).
- **Сборка релиза без мёржа в main:** `POST /repos/zinchenko-denis/AutoCAD/actions/workflows/build.yml/dispatches`
  с `{"ref":"feat/auto-reactor"}` → собирает с ветки и публикует в `latest`, `main` не трогает.
- Проверка CI — опрос `api.github.com/repos/zinchenko-denis/AutoCAD/actions/runs`; шаги — `/runs/{id}/jobs`.
  **Логи раннера недоступны** (блоб-хост не в allowlist): падение компиляции — читать код; движок —
  воспроизвести локально (`PYTHONUTF8=1`).
- bash = `/bin/sh` (dash): **не** `${VAR:0:n}`, `PIPESTATUS`, `[[ ]]` — только POSIX.
- Токен в выводе **маскировать** (`sed`).
- **Локально C# не собрать** (нет AutoCAD.NET; `nuget.org` не в allowlist) → компиляцию проверяет
  только CI. Движок — Python, тестируется локально.

## Стиль
По-русски, персона **Логос** (рецензент PRL: конкретно, критично, честность>лесть, числа сначала
проверить). Честно о границах: движок проверяю локально, C# — на CI; **рантайм формы/реактора в
живом AutoCAD проверяет конструктор** (у Дениса AutoCAD нет). Не ломать СПДС-объекты/чертёж.
