# ABlockGen — состояние и хэндофф (ЧИТАТЬ ПЕРВЫМ)

> **Это проект ABlockGen (быв. VitrageGen; имя — решение Дениса 13.07:
> английское, «построитель блоков», парно к ATableSpec) — генератор
> витражей/конструкций из блоков, сосед ATableSpec в монорепо
> `zinchenko-denis/AutoCAD`. НЕ путать с LD/физикой.** Это ОТДЕЛЬНАЯ
> программа (свой бандл ABlockGen.bundle, своя DLL ABlockGenPlugin, свой
> движок) — с ATableSpec связана только блочным контрактом и общим репо
> (монорепо: общий CI и тест-данные — решение 11.07). Мандат ATableSpec —
> `ATableSpec/docs/NEXT.md` (грабли среды/сборки/push — ТАМ, здесь не
> дублируются и обязательны к прочтению). Ветка общая: `feat/auto-reactor`.

## TL;DR
- **Что:** чертёж АР (DWG/DXF) → витраж/светопрозрачная конструкция ИЗ БЛОКОВ.
  Отдельная программа со своим бандлом и py-движком (гибрид-паттерн ATableSpec:
  тонкий C# + замороженный exe, JSON через временные файлы).
- **Зачем отдельная:** направление данных обратное (ATableSpec: блоки→таблица;
  здесь: геометрия→блоки), движок отчётов заморожен и в бою, риск-профили и
  релизные циклы несовместимы. Решение зафиксировано 11.07.
- **Связка:** блочный КОНТРАКТ (`docs/CONTRACT.md`) — генератор выпускает ту же
  номенклатуру (слои RF-*, ИМЯ/ПРОФ/ДЛИНА/МАРКИРОВКА/РАЗМЕР_ЗАП, анонимно-
  динамические вставки), которую ATableSpec ест без правок. Acceptance связки:
  «сгенерил витраж → спецификация и раскрой сходятся».
- **Статус (13.07, старт проекта):** Э1-скелет ПОЛНОСТЬЮ написан: движок +
  45 юнитов + 33 сверки с эталоном (зелёные локально), C#-команда ATVITRAGE
  (mcs --parse чистый; полная компиляция — CI check), бандл-манифест, CI
  расширен. НЕ выкладывалось, Алексей не видел. Рантайм в AutoCAD не проверялся.
- **Статус (13.07b, Э3-прототип по реальному АР):** Денис прислал «Проба 3.dxf»
  (АР-витраж Revit-вставками + ручной КМД-эталон Алексея) с workflow:
  «Укажите блок стоек → блок ригелей → конструкцию» → каркас. РЕАЛИЗОВАНО:
  движок `vitrage_recognize.py` (32 юнита + 23 D2-сверки: распознанный план
  == ручной каркас Алексея, оси ≤0.5 мм, ДЛИНА байт-в-байт), команда
  **ATVITRAGEAR** (`RecognizeCommand.cs`), единый CLI `vitrage_engine.py`
  (op: plan|recognize). Файл — `atspec-testdata/dxf/ar/Проба_3.dxf`.
  Правила распознавания — CONTRACT.md §Э3. Заполнения/двери/окна Э3 НЕ ставит —
  каркас only (двери/окна вставляют утилиты Алексея; его постановка).
- **Статус (13.07c, решения Дениса по развилкам + доделки):** Э1 — всё как
  реализовано (отметки списком мм; марки по типоразмерам; ДЛИНА ригеля =
  осевой шаг подтверждён; обвязочные — списком). Имя: **ABlockGen**.
  Э3-решения: (A) **опциональный образец ВЕРХНЕГО ригеля** — третий запрос
  ATVITRAGEAR (Enter — обычный): вся верхняя отметка идёт этим блоком
  В СВЕТУ между телами (как Р31/Р33 эталона); (B) деталировка — дефолт
  (исходное определение, «0», пишем только ИМЯ/ПРОФ/ДЛИНА); (C) марки —
  всегда с 1; (D) разрывы вертикалей — сливать (note); (E) **сборка полос
  из ГОЛЫХ ОТРЕЗКОВ** заложена сразу: `segments[]` в запросе, пары
  параллельных отрезков (40..120 мм, перекрытие > max(w,50)) → полосы,
  дедуп дублей; C# шлёт LINE и ломаные полилинии сегментами, замкнутые
  ≤5 вершин — bbox-полосой, дуги пропускает. Итог: **47 юнитов + 34
  D2-сверки — план с rigel_top воспроизводит ручной каркас Алексея
  ПОЛНОСТЬЮ (все 11 ригелей, включая верхние в свету, ДЛИНА байт-в-байт);
  R11 — тот же витраж «голыми линиями» распознаётся идентично.**

- **Статус (14.07, первый рантайм-фидбэк Алексея: «Привет! Работает!») —
  заход по двум пунктам:**
  **(1) БАГ rot=270 у ригелей (Алексей повернул вручную) — КОРЕНЬ НАЙДЕН
  И ЗАКРЫТ:** rot=270 был скопирован с контракта Проба_штапики_2, где
  определение ригеля 17_06_01 рисовано ВЕРТИКАЛЬНО; у Алексея обычный
  ригель (F50.02.03) рисован ГОРИЗОНТАЛЬНО → rot=0, а верхний Р31/Р33
  (профиль стойки F50.01.07) — вертикально → rot=270 (факты его же
  эталона; точка вставки инвариантна — (левый конец, ось)). Поворот —
  свойство ОПРЕДЕЛЕНИЯ, не конструкции. ФИКС: rot приходит от C# —
  ATVITRAGEAR берёт ПОВОРОТ ОБРАЗЦА (PickSample → NormDeg), ATVITRAGE —
  МОДУ поворотов существующих вхождений определения в чертеже
  (ModeRotation); дефолт движка 0. D2 теперь сверяет и повороты с фактом.
  **(2) РАЗМЕРЫ на чертеже (предложение Алексея — «сразу строить»):**
  движок кладёт в план `dims[]` (h-цепочка межосевых стоек; габарит
  ширины по телам; v-цепочка ОТ ГАБАРИТА по осям ФАКТИЧЕСКИ поставленных
  ригелей до габарита — 300/2400/900/300/285 на «Проба 3», порог двери
  в цепочку не попадает; габарит высоты). `build_dims` общий (vitrage_plan),
  отступы max(300, 8×тело) и ×2. C#: RotatedDimension на слое «Размеры»
  текущим стилем чертежа, в той же транзакции (один undo); ShiftPlan
  двигает dims вместе с каркасом; сводка «; размеров N».
  Тесты: **54+33 (plan) и 53+49 (recognize)** — D2 сверяет позиции, ДЛИНА
  байт-в-байт и ПОВОРОТЫ (0/270) с ручным каркасом Алексея.
  Алексей: «на подобных витражах отрабатывает на 5» — готовит АР других
  архитекторов («импровизации», надо натаскивать) — ЖДЁМ ФАЙЛЫ в
  `atspec-testdata/dxf/ar/`.

- **Статус (15.07, фидбэк Алексея по #57 + два его АР-«образца»):**
  Алексей: **«всё норм, ригеля ставит как надо»** — rot-фикс и размеры #57
  ПРИНЯТЫ. Два пожелания, оба СДЕЛАНЫ на ветке:
  **(1) КНОПКИ КОМАНД (лента + классический тулбар):** перенос отработанного
  UI ATableSpec → `RibbonUi.cs` (вкладка «ABlockGen», 3 кнопки) и
  `ClassicUi.cs` (меню + плавающий тулбар, BMP по полному пути, «_CMD » без
  ^C^C, позиция v3: Idle+таймер, реестр HKCU\Software\ABlockGen). Кнопки:
  ATVITRAGEAR «Витраж по АР», ATVITRAGE «Витраж по проёму», **ATSPECREPORT
  «Отчёт»** (кросс-команда ATableSpec — просьба Алексея; без ATableSpec даст
  «Неизвестная команда», ничего не ломает). Иконки `abg_ar/abg_grid` (новые,
  стиль ATableSpec) + `abg_report` (копия ats_report) — PNG 32 + BMP 16/32
  на фоне 64,64,64; build.yml копирует `Contents/icons` в бандл;
  PackageContents.xml дополнен ATVITRAGEAR. csproj: +WPF/Microsoft.CSharp.
  РАНТАЙМ-ПРОВЕРИТЬ Алексею: вкладка ленты, тулбар в классике, кнопка
  «Отчёт» дергает ATableSpec, позиция тулбара переживает перезапуск.
  **(2) ЗАПРОС ВЕРХНЕГО РИГЕЛЯ УБРАН** («делать по умолчанию как остальные»):
  третий PickSample удалён, rigel_top в запрос не шлётся — вся верхняя
  отметка обычным ригелем ОСЕВОГО шага. Функция в движке СОХРАНЕНА
  (юниты R9/R12/D2 живы) — вернуть при необходимости = один PickSample.
- **Статус (15.07b, «дообучение» на 2 АР-импровизациях — ГЛАВНОЕ сессии):**
  Денис прислал `Образец 1.dxf` (АР голыми ЛИНИЯМИ: стойки-пары 100 мм,
  дверь линиями, размерные выноски) и `Образец 2.dxf` (Revit-вставки:
  импосты 70/100/150/200, дверь ADSK на подставке, ДВЕ рамки обрамления,
  высотная отметка) — оба с ручным RF-эталоном Алексея в том же файле.
  Ни один не распознавался. ПАКЕТ ДВИЖКА (все правила доказаны эталонами):
  (а) union-сборка полос: сегменты одной координаты (±0.5) сливаются в
  линии, пары линий → полосы кусками пересечения union'ов (дроблёный
  контур стойки Образца 1 собирается сквозь разрывы на ригелях);
  (б) РАМКИ ОБРАМЛЕНИЯ: замкнутые прямоугольники, охватывающие ≥50% полос
  (двухпроходно: полосы → кандидаты по охвату → полосы без пар
  «рамка×рамка») — задают низ/верх каркаса ЖЁСТКО (Образец 2: стойки 3334
  от высотной отметки, не 3470 по графике) и убивают фантомы 90/160 мм;
  (в) ФАНТОМ-ФИЛЬТР сегментных вертикалей: ≥2 «якорей» (полоса-отметка
  рядом по X примыкает к торцу куска цепочки или проходит сквозь) — дверные
  коробки/полотна (0 якорей) отсеиваются; хвосты размерных выносок
  (куски вне пояса горизонталей) отрезаются;
  (г) габарит стоек: при ≥2 отметках стойка тянется сквозь обвязку до
  общего габарита (Образец 1: 2850 байт-в-байт);
  (д) обвязка ВНЕ рамки → клэмп к краю рамки ± тело/2 (Образец 2: низ
  32868.7 = рамка+25, дедуп совпавших отметок);
  (е) wmax 120→210 для ВСТАВОК (Revit-импосты 150/200), для сегментных
  пар — строже 120 (`seg_w_max`; на 150+ голые линии дверных проёмов
  дают ложные отметки);
  (ж) `door_pat` «дверь»→«двер» (стем: «ADSK_Двери_…» не содержало
  подстроку «дверь» — дверь Образца 2 не ловилась!); зона порога вниз
  расширена 60→wmax (дверь на подставке: полоса порога на 75 ниже полотна);
  (з) body_w — С ОБРАЗЦА БЛОКА: C# шлёт поперечник графики определения
  (`BodyWidth`, min(w,h) bbox без текстов; тело 50 при АР-полосах 70..200);
  (и) правило крайних осей: прижим к внутренней грани, если крайняя полоса
  шире ТИПИЧНОЙ ВНУТРЕННЕЙ +5 (Образец 1: все полосы 100 → оси по центрам,
  как строит Алексей; Образец 2/Проба 3: крайние шире → грань ∓ тело/2).
  ИТОГ СВЕРКИ с эталонами: **Образец 1 — ПОЛНЫЙ матч (3 стойки + 5 ригелей,
  позиции/ДЛИНА байт-в-байт); Образец 2 — 4 стойки + 8 ригелей, всё
  байт-в-байт КРОМЕ верхней обвязки: эталон −25 от оси полосы (прижат к
  верху рамки)**. Тесты: **72 юнита recognize (было 53: +R14 «Образец 2»,
  +R15 «Образец 1») + D3-сверка живых образцов (60 сверок) + 54 plan +
  32 crash — зелёные.** D3 ищет файлы в `atspec-testdata/dxf/ar/` и
  `/home/claude/ar_samples/`.
- **Конвенция обвязки — ЗАКРЫТА решением Алексея 15.07** (на наш вопрос
  «Образец 1 ставит по осям АР-полос, Образец 2 — по краям рамки, единого
  правила нет»): **«Предлагаю делать по габаритам — по краю рамки ставить
  край блока, необходимые изменения потом будут выполняться вручную».**
  РЕАЛИЗОВАНО: отметка, чья полоса ДОСТИГАЕТ края габарита каркаса (рамка
  обрамления, иначе габарит стоек), ставится краем тела на край: ось =
  край ± body/2; середины и отметки в поле (порог «Пробы 3») — по осям
  АР-полос. Итог сверок: Образец 2 — ПОЛНЫЙ матч (вкл. верх 36152.7);
  Образец 1 — обвязка теперь 29011.5/31811.5 при старом ручном эталоне
  29026.5/31786.5 (±15..25) — по решению Алексея расхождение штатно
  («правится вручную»), D3 держит допуск 25.5 ТОЛЬКО для крайних отметок.
  «Проба 3» не задета: её отметки в поле, D2 49 сверок байт-в-байт.
- **DXF-образцы 1/2 — ПОЛОЖЕНЫ в `atspec-testdata/dxf/ar/`** (коммит
  `57c7df7` 15.07): D2 + D3 живые сверки идут при клоне testdata токеном.
  Полный живой прогон 15.07: **54+33 (plan) / 72+49 D2+60 D3 (recognize) /
  32 (crash) — зелёные.**
- **СБОРКА rot-фикса — ВЫЛОЖЕНА (#57, 14.07 ~21:2x UTC, оба бандла свежие:
  ABlockGen 7.77 МБ sha256:64888102…, ATableSpec 24.3 МБ sha256:3cce8cbc…).
  Апдейт-инструкция `docs/ABlockGen_upd_1407.pdf` — отправлена, Алексей
  проверил 15.07: ПРИНЯТО.**
- **Статус (14.07b, краш-тест перед тестированием Алексея):**
  `engine/test_crash_scenarios.py` — 25 сценариев практики (грязные/дублевые/
  дрожащие полосы, смешанные ширины, двери-краевые, координаты 1e7, штормы
  50×30, битый JSON, round-trip кавычек): **32 проверки OK, 3 осознанных
  EDGE, багов движка НЕ найдено**. EDGE зафиксированы в тесте: (K6) колонна
  из сегментов РАЗНОЙ ширины → внятная ошибка «оси близко», не кривой каркас
  (лечить по реальному АР); (K10) дверь глушит и порог (≥50% света, граница
  включительно), и отметки внутри своего Y-габарита; (K13) шаблон марки без
  {n} — одинаковые имена молча. Добавлен в CI check. Полный регресс:
  54+33 / 53+49 / 32 — зелёные.
- **ГРАБЛЯ-5 (переписана по фактическому уроку 14.07, ЧИТАТЬ ПЕРЕД «Собираем»):**
  в среде Cowork неавторизованное чтение github.com (WebFetch: /actions,
  /releases, expanded_assets) отдаёт STALE-КЭШ ЧАСАМИ. 14.07 из-за этого
  был поставлен ЛОЖНЫЙ диагноз «раны не создаются» (свалено на реальный,
  но непричастный инцидент GitHub) и сделано 8 ЛИШНИХ сборок (#48–#55 —
  все Success, ассеты обновлялись с 14:23, а «наблюдалось» 13.07 21:11).
  ПРОТОКОЛ: (1) после «Собираем» — ОДИН пуш build-trigger; (2) раны идут
  стабильно 2–3 мин; (3) если неавторизованная проверка не видит свежий
  ассет — это НЕ доказательство провала: НЕ перезапускать, а попросить
  у Дениса скриншот /actions (его залогиненный взгляд — единственный
  достоверный канал); (4) максимум ОДИН повторный пуш за заход, никаких
  многочасовых циклов. Push-канал работает (в т.ч. токен-в-URL 14.07
  проходил). Ручной «Run workflow» — ТОЛЬКО Branch: feat/auto-reactor
  (запуск с main пересобирает ассеты ДРЕВНИМ кодом — прецедент #56,
  закрыт пересборкой #57).
## Этапность (решение 11.07; каждый этап — рабочий продукт)
- **Э1 (ТЕКУЩИЙ): полуавтомат БЕЗ распознавания** — проём рамкой/контуром +
  диалог параметров (шаг стоек, отметки ригелей, ярусы, блоки) → сетка
  стоек/ригелей/заполнений ВХОЖДЕНИЯМИ существующих определений блоков чертежа
  (как Алексей руками; свои определения НЕ выдумываем).
- **Э2:** автозахват габарита проёма кликом внутрь контура.
- **Э3:** членение по нарисованным импостам АР.
- **Э4:** пакетно по фасаду.
- Гейт Э2/Э3 — **2–3 реальные АР-подложки от Алексея** (DWG→DXF; класть в
  `atspec-testdata/dxf/ar/`). Наблюдение: в эталоне есть АР-слои
  `_АР_ВИТРАЖ_*` со структурой (см. CONTRACT.md) — если реальные АР такие,
  Э3 сильно упрощается; но источник один, вердикт рано.

## Архитектура (гибрид — как ATableSpec)
- `src/ABlockGenPlugin/` → `ABlockGenPlugin.dll` (net48, AutoCAD.NET 24.0.0,
  JavaScriptSerializer, WinForms). Файлы: `Plugin.cs` (IExtensionApplication,
  минимум), `VitrageCommand.cs` (ATVITRAGE), `VitrageForm.cs` (диалог).
- `engine/vitrage_engine.py` — единый CLI (op: plan → vitrage_plan.build_plan,
  recognize → vitrage_recognize.recognize); при релизе замораживается в
  `vitrage_engine.exe <req.json> <out.json>`. Движок ЧИСТЫЙ (никакого DXF):
  вся работа с чертежом — на стороне C#. Файлы: `Plugin.cs`,
  `VitrageCommand.cs` (ATVITRAGE, Э1), `RecognizeCommand.cs` (ATVITRAGEAR, Э3),
  `VitrageForm.cs` (диалог Э1).
- Поток ATVITRAGE: GetPoint+GetCorner (проём, UCS→WCS) → список определений
  блоков чертежа → диалог (предвыбор эталонных 17_07_24/17_06_01/СТП) →
  JSON-запрос → exe → JSON-план → вставка вхождений одной транзакцией
  (= один undo): BlockReference + динсвойства ДО атрибутов (растяжка меняет
  представление; атрибуты сеются из br.BlockTableRecord ПОСЛЕ установки dyn).
- JSON-план: `inserts[] = {kind, block, layer, x, y, rot, dyn{}, attrs{}}` —
  C# вставляет один цикл без ветвлений по kind; `summary`, `notes[]`.

## Правила Э1 (в движке, доказаны эталоном — см. CONTRACT.md)
- Оси стоек: крайняя = край проёма + тело/2; далее шаг слева направо
  (остаточный последний пролёт + note) ЛИБО n_cols равных долей.
- Ярусы стоек: список длин + зазор (терморазрыв 10); пусто = вся высота.
  Несходимость сумм с высотой проёма — note, считаем как задано.
- Ригели: на каждой отметке (осевые Y от низа, список пользователя) в каждом
  пролёте; «Длина» и атрибут ДЛИНА = ОСЕВОЙ шаг пролёта.
- Заполнения: ячейки светов; свет < min_fill (50) — пропуск (щель);
  РАЗМЕР_ЗАП = округл(свет)+fold(15), Х кириллическая (U+0425).
- Марки: по типоразмерам (одинаковый размер = одна марка): С{n}/Р{n}/Сп{n},
  шаблоны в диалоге.

## Тесты
- `engine/test_vitrage_plan.py` — 45 юнитов (синтетика, в CI) + D-сверка с
  живым эталоном `Проба_штапики_2.dxf` при его наличии (субсет: каждая
  вставка плана обязана существовать в факте; позиции ≤0.5 мм, ДЛИНА и
  РАЗМЕР_ЗАП байт-в-байт) — 33 сверки локально зелёные.
- CI `check.yml`: paths + сборка ABlockGenPlugin.csproj + юниты движка.
- Acceptance связки (СДЕЛАТЬ после первого рантайма): сгенерированный
  ATVITRAGE чертёж → ATSPECREPORT «Спецификация»/«Заполнения»/«Штапики» —
  таблицы сходятся с ручным расчётом.

## Открытые хвосты
- **РАНТАЙМ-ПРОВЕРКА Алексеем (первая, критичная):** на «Проба 3» —
  ATVITRAGEAR: образец стойки → образец ригеля → рамка на красную зону →
  Enter (на месте) или точка: каркас 4 стойки 4185 + 11 ригелей, порог
  под дверью пропущен, оси как в его зелёном эталоне. Затем ATVITRAGE (Э1)
  на чертеже с блоками 17_*/СТП. Общие риски: (а) динрастяжка «Длина»
  сработала (не 1000); (б) атрибуты заполнены и не уехали после растяжки
  (сеем из представления ПОСЛЕ dyn — локально не проверить); (в) ATSPECREPORT
  на сгенерированном каркасе читает стойки/ригели.
- **ПЕРВАЯ СБОРКА ВЫЛОЖЕНА (13.07 ~21:12 UTC):** build.yml расширен —
  собирает и публикует ОБА бандла в `latest`; build-bundle **#45** (ветка
  build-trigger = feat/auto-reactor `c542c9e`, обход ГРАБЛИ-4) Success 1m55s;
  ассеты подтверждены страницей `releases/expanded_assets/latest` (ход мимо
  lazy-JS — рабочий приём верификации): **ABlockGen.bundle.zip 7.76 МБ**
  (2026-07-13T21:11:54Z) + ATableSpec.bundle.zip 24.3 МБ (пересборка того же
  кода f80928e, не деградация). Заморозка vitrage_engine.exe — в составе
  сборки (PyInstaller onefile, только stdlib). Инструкция Алексею:
  `docs/ABlockGen_guide_1307.pdf` (генератор `make_ablockgen_guide.py`) —
  установка + обе команды + приёмка на «Проба 3» + ограничения; отправлена
  Денису (переслать Алексею).
- ~~CI-раны #55/#56 — допроверить~~ — **ЗАКРЫТО сильнейшей проверкой:**
  build-ран #45 выполнил ПОЛНЫЙ `dotnet build` обоих csproj (ATableSpec +
  ABlockGen с RecognizeCommand и всеми правками 13.07) и PyInstaller-заморозку
  движка — компиляция всего кода сессии подтверждена. Статусы check #55/#56
  сами по себе больше ничего не решают (тот же код собран build-раном).
- **Створки/доборники/кронштейны** — не генерируются (Э1: сетка).
- **Повёрнутый UCS** — v1 строит в мировых осях, предупреждение в ком. строке.
- **Верх/низ без ригеля:** ячейка света идёт до края проёма. Обвязочные
  ригели автоматически НЕ ставятся — пользователь задаёт отметки сам.

## Развилки — ЗАКРЫТЫ Денисом 13.07 (решения)
Э1: (1) отметки ригелей — СПИСКОМ мм (как реализовано); (2) марки —
по типоразмерам, л/п и типы заполнений НЕ тянем; (3) ДЛИНА ригеля =
осевой шаг — ПОДТВЕРЖДЁН по эталону; (4) обвязочные — списком отметок;
(5) имя — **ABlockGen** (переименовано 13.07).
Э3: (A) опциональный образец верхнего ригеля — СДЕЛАНО (третий запрос,
Enter — обычный); (B) деталировка — дефолт, СДЕЛАНО; (C) марки всегда
с 1 — так и было; (D) разрывы — сливать (дефолт), так и было;
(E) полосы из голых отрезков — ЗАЛОЖЕНО сразу (`strips_from_segments`).
Открытым остаётся только вопрос ПРАКТИКИ (не кода): как Алексей решает,
ГДЕ нужен верхний ригель-в-свету — уточнится на его рантайм-проверке.

## Рабочий процесс
Всё общее (среда, POSIX-шелл, токен, ГРАБЛИ-1..4, push с extraHeader,
верификация CI через WebFetch, «Собираем»/«Пушим») — в
`ATableSpec/docs/NEXT.md`, раздел «Рабочий процесс / грабли». Специфика
ABlockGen: коммит-префикс `витраж:`; движок тестируем локально
(`PYTHONUTF8=1 python3 engine/test_vitrage_plan.py`), C# — mcs --parse
локально + полная компиляция на CI check; рантайм — только у Алексея.
