# AFrame — состояние и хэндофф (ЧИТАТЬ ПЕРВЫМ)

> **Это проект AFrame — подсистема НВФ (этап 3 фасадного направления):
> кронштейны + направляющие + кляммеры по согласованной раскладке
> AClad. НЕ путать с LD/физикой.** Концепт и решения —
> `Facades/docs/STAGE3_FRAME.md` (В13–В20 закрыты Денисом/фактами
> боевого DWG 24.07). Грабли среды/сборки — `ATableSpec/docs/NEXT.md`;
> методика фидбэка — `ABlockGen/docs/NEXT.md`. Ветка общая
> `feat/auto-reactor`; `main` — только по «Пушим».

## TL;DR
- **Что:** команда **ATFRAME** (этап Э2, ещё не написана) — монтажные
  схемы подсистемы по зонам с меткой ATCLAD: стойки по осям
  вертикальных рустов, несущие кронштейны на отметках перекрытий
  (центр — В15), рядовые равномерно (В16), кляммеры по горизонтальным
  швам; условные знаки СВОИ (В13: DWG блоков Герман даст позже),
  слои Германа `_01_ПС_*` (В19).
- **Порядок целевой (Денис 24.07): расчёт несущей способности → шаги
  → расстановка.** Пока расчёта нет — шаги из справочника систем
  (systems.json: Вектор-1 / Standart / Межэтажная) с подтверждением
  конструктором; модуль расчёта (этап 4) встанет ПЕРЕД движком и
  заменит дефолты.
- **Статус (24.07, Э1 ГОТОВ):** движок `engine/frame_plan.py`
  (op=frame: стойки рвутся проёмами по bbox, сегменты режутся
  отметками перекрытий, стык направляющих центрован на отметке с
  зазором системы; несущие/рядовые/кляммеры; summary с м.п. и
  прикидкой хлыстов) + `systems.json` + **22 юнита** (FR1–FR6 —
  числа руками; FR-D — факты боевого эталона
  `atspec-testdata/dxf/facades/frame_lenprospekt/frame_ps.json`:
  кляммеры 605, кронштейн ~300+~950, стойки 602.5–605). C#/бандла НЕТ.

## Файлы
- `engine/frame_plan.py` — движок Э1 (докстринг = модель данных);
- `engine/frame_calc.py` — **движок этапа 4** (расчёт несущей
  способности по методике «Вектор фасад», 13 боевых статрасчётов;
  конспект — `atspec-testdata/docs/facades_calc/METHOD_CALC.md`);
- `engine/test_frame_calc.py` — 71 проверка (эталоны = числа их PDF);
- `engine/systems.json` — справочник систем (правила = атрибуты
  системы, вывод из дельты КАФТ↔Вектор);
- `engine/test_frame_plan.py` — 42 проверки (`PYTHONUTF8=1`, из
  каталога engine; FR-D скипается без testdata).

## Дальше (порядок ценности)
1. **Э2 — команда ATFRAME + бандл AFrame 5-м в build.yml**: чтение
   метки ATCLAD (оси из кромок камней; в метку AClad дописать
   joints_x[]/rows_y[] при ближайшей правке), диалог (система,
   отметки перекрытий кликами, подтверждение шагов), блоки-знаки в
   бандле (квадрат/ромб с крестом в круге r75 — по образу Германа,
   какой знак несущий — спросить), вставки на слои `_01_ПС_*`, метка
   «ATFRAME», перегенерация, clad_engine-паттерн CLI (frame_engine.py).
2. Прогон Германа на его фасаде → фидбэк → правки; его ручной эталон
   подсистемы на нашем тестовом фасаде → полная D-сверка.
3. Ведомость подсистемы через ATableSpec (блочный контракт:
   МАРКИРОВКА/ЗАХВАТКА у знаков).
4. Этап 4 — модуль расчёта (методики от Дениса) → шаги считаются.

## Открытое
- **В-н (этап 4):** floor-правило констант неразрезной балки и
  консервативная трактовка угловой Новгорода — подтвердить у Германа
  (их эксель местами непоследователен: рядовая 2-пролётные, угловая
  3-пролётные при одинаковой геометрии).
- **В-о (этап 4):** конструктивные MAX шага кронштейнов по системам
  (расчёт монолита пропускает и 1000+, но их вывод 800 — предел из
  АТР?): ГП-40-40→800? ШП-90-20→1200? — таблица от Германа.
- **В-п (этап 4):** высотные диапазоны лестницы шагов (0–24/24–45/
  45–65 у Нижнекаменской) — фиксированные пороги или от проекта?
- Какой из двух знаков Германа = несущий (квадрат с X-крестом или
  ромб с прямым крестом) — спросить при первом прогоне.
- Половинки кляммеров на краях/примыканиях — Э2+ (сейчас не ставятся).
- Угловые спецузлы (крест-накрест, короба) — сознательно НЕ моделируем
  (В17: типовой случай, угловая зона = полоса шириной ~плитка).

## Статус
- **27.07a — ЭТАП 4 НАЧАТ: движок frame_calc.py по 13 боевым
  статрасчётам «Вектор фасад» 2022–2026 (загрузил Денис; PDF + конспект
  METHOD_CALC.md в `atspec-testdata/docs/facades_calc/`, коммит
  f067f77). Методика ВОССТАНОВЛЕНА ПОЛНОСТЬЮ, вся арифметика всех
  блоков сверена руками:** пиковая ветровая СП 20.13330.2016
  (k/ζ степенные, тип A и B подтверждены на их числах, γ_в=1.4,
  c_p −1.2/−2.2), гололёд+два сочетания, грузовые площади по схемам
  (vertical: b×L1; interfloor: L2×L_напр; ortho: b×v_step),
  «k_нер» = коэффициент опорной реакции неразрезной балки — пары
  констант по числу пролётов (2: 0.125/1.25/0.0052; 3:
  0.100/1.10/0.00675; ≥4: 0.106/1.132/0.0063; правило n=max(2,
  floor(L/шаг)), подтверждено даже на нестандартном 450→6→1.132
  республиканской), анкер трёхчлен N_п·e1/e2+N_в·e3/e4+N_в (плечи =
  константы кронштейна, e1=вынос−Δ), кронштейн 1-1/2-2, удлинитель,
  профиль σ+прогиб (от НОРМАТИВНОЙ ветровой, f≤L/150), заклёпки
  срез/смятие. Справочник сечений: КР2-70/КР1-70/КР1-85/КП-125,
  УК-70/УК-85, ГП-40-40/ЗП-40-20/НСП-95-70/НСП-69-60/ШП-90-20/
  ШП-60-20(-20)/НШП-2. **71 юнит зелёный** — эталоны: монолит/кирпич
  (анкер 104/140≤306.1/244.9, кронштейн 1157/2024, профиль 452.9/817.7),
  Нижнекаменская тип-4 (281.2/277.6, 2080.9/2145.2, прогиб 7.9/8.2),
  Искра тип-5 (231.8/216.2, e3=0, 4 заклёпки), республиканская
  (кронштейн 1997/2039 — почти упор; **pick_step воспроизводит ИХ
  ВЫВОДЫ 800/450: при 500 кронштейн 1-1 валится**), Новгород-260
  ортогональная (72.4≤130.6; в углу их эксель непоследовательно взял
  3-пролётные — движок консервативнее по floor-правилу, эталон угла
  пересчитан руками), Регенбоген тип A. Соответствие схем: Тип-1 =
  наша vertical, Тип-4/5 = interfloor (5 без удлинителя, КП-125,
  e6=27), Новгород = ortho. Бугры-2018 = ПРЕДЫДУЩЕЕ поколение той же
  таблицы (γ_обл 1.2→1.1, R_y 2136.4→2250, z_e 15→факт. высота, без
  гололёда/k_нер, f≤L/200→L/150) — НЕ эталон. Реализационные решения:
  w_eff = max(ветер; гололёд+0.6·ветра) внутри цепочки; конструктивный
  max шага = вход max_step (дефолт 800). C#-интеграции НЕТ (следующий
  заход: шаги frame_calc → frame_plan вместо справочника, сокращение
  диалога ATFRAME; лестница высотных диапазонов как у Нижнекаменской
  350/400/500 по 24/45/65 м). Вопросы В-н..В-п — §Открытое.**
- **26.07e — «Собираем» №8 (Денис): build-trigger `a326397` = 020a2f1
  (весь пакет: 3 типа подсистемы + образцы + ATCLADDIM + лента).
  PDF Герману AFRAME_update_2607_2.pdf (что сделано по его письму,
  допущения v1 честно, установка всех трёх бандлов, тест-план по
  типам, вопросы) + генератор. СЕССИЯ ЗАВЕРШЕНА ХЭНДОФФОМ: промт
  переноса ОБНОВЛЁН (Facades/docs/HANDOFF_PROMPT.md, версия 26.07 —
  задача новой сессии: фидбэк по трём типам + ЭТАП 4 расчётный
  модуль, материалы загрузит Денис; копия с токеном отдана Денису
  файлом). Следующая сессия стартует по нему.**
- **26.07d — ПАКЕТ ПО КОМАНДЕ ДЕНИСА (без сборки — «соберём после
  прогонов»): МЕЖЭТАЖНАЯ и ОРТОГОНАЛЬНАЯ v1 + образцы блоков +
  ATCLADDIM + ЛЕНТА «Фасады» ×3.** Движок: sub_type
  vertical/interfloor/ortho; межэтажная (FR4/FR11): кронштейны по
  центру перекрытия с шагом ПО ГОРИЗОНТАЛИ (_hpos: углы 1500 чаще,
  крайние 150 от краёв, точки в проёмах пропускаются), НГП
  горизонтальный (hrails, рвётся окнами, note>6000), НСП в рустах
  БЕЗ смещения у окон (куски над/под окном = ШП-60-20), вставки на
  стыках НСП×НГП, СП-60-40 в подоконной зоне со скобами С1 (fittings);
  ортогональная (FR12/FR13): сетка кронштейнов (верт. 600 от
  низа+300, гориз. расчётный), ГП-40-40 на каждом ряду, ШП-60-20 по
  рустам, Z-профиль у окон (смещение 100, выступ 50), свес ≤300
  note. В-е (Денис): деление несущий/опорный в вертикальной ЕСТЬ —
  эвристика «первый от стыка = несущий» остаётся до слова Германа.
  Пресеты: Межэтажная наполнена, Ортогональная добавлена. Тесты
  42+5. C#: диалог «Тип подсистемы [Вертикальная/Межэтажная/
  Ортогональная]», отрисовка hrails прямоугольниками и метизов
  блоками (AFRAME_ВСТАВКА/AFRAME_СКОБА), кляммеры 4 видов (боковой
  с ножкой вбок, комбинированный с двумя), «Знаки [Условные/
  Образцы]» — образцы НЕСУЩЕГО/ОПОРНОГО кронштейнов с чертежа
  (DynamicBlockTableRecord, библиотека Германа «Кронш …»). AClad:
  команда ATCLADDIM (Ряд/Столбец → размерная цепочка по камням через
  GeometricExtents на слой _РАЗМЕРЫ_ОБЛ, паттерн AddDim ZoneCommand;
  в PackageContents добавлена). ЛЕНТА: FacadesRibbon ×3 модуля —
  ОБЩАЯ вкладка «Фасады», каждый плагин пересоздаёт только СВОЮ
  панель (Зоны+Ведомость / Раскладка+Размеры / Подсистема), иконки
  PNG в бандлах (Contents/icons, светлые под тёмную тему — урок
  ATableSpec 25.06), build.yml копирует icons. mcs все файлы OK,
  дубли чисто, хелперы OK. Рантайм НЕ проверялся, сборки НЕ БЫЛО.
  Допущения v1 и В-к/В-л/В-м — FRAME_TZ.md.**
- **26.07c — БОЛЬШОЕ ТЗ ГЕРМАНА (письмо, дословно в docs/FRAME_TZ.md)
  + ЕГО БИБЛИОТЕКА БЛОКОВ (testdata blocks/Bloki_dlya_raskladki_
  German_2607.dwg: «Кронш КР1/КР2/КПК/КО-<полка>-<вылет>», кляммеры
  рядовой/стартовый/половинка/угловой/комбинированный, динпрофили
  «Тип 1 и 2 / Тип 4 Вектор» 3000 база низ-центр, ВС-300/В-70/скоба
  С1; примеров «Огни залива» в файле НЕТ — запрошены В-и).
  РЕАЛИЗОВАНА ВЕРТИКАЛЬНАЯ СИСТЕМА ПО ТЗ: кронштейны НА КАЖДУЮ
  направляющую (300 от низа, 300 от верха, между равномерно ≤ шага;
  3000 → 300-800-800-800-300 ✓ FR1c), угловая зона 1500 (все
  системы), оконная стойка [низ−50, верх+50] (длина = сторона окна +
  100 ✓ FR7), доп. ось по центру плиты шире 600 (mid_rail_over=650 ✓
  FR10), кляммеры по видам: БОКОВОЙ на оконных стойках (В-а закрыт
  ТЗ: у отлива боковые), КОМБИНИРОВАННЫЙ на первом шве после стыка-
  термошва (✓ FR3), стартовый низ зоны/над откосом; «несущий» = 1-й
  кронштейн направляющей от стыка (В-е — уточнить); C#: вопрос
  «Кляммеры [Да/Нет]» (только керамогранит). Тесты 29+5 зелёные,
  mcs OK. МЕЖЭТАЖНАЯ и ОРТОГОНАЛЬНАЯ — концепт в FRAME_TZ, генераторы
  СЛЕДУЮЩИМИ заходами (НГП/НСП/ШП/СП/скобы С1 — блоки уже есть).
  В очереди также: образцы блоков Германа вместо наших знаков
  (+вылет/маркировка кронштейнов — ТЗ п.3), ATCLADDIM «размеры
  облицовки» (AClad), лента с иконками для всех модулей (прецедент
  RibbonUi ATableSpec). Вопросы: В-е, В-ж (боковой=КЛУ?), В-з
  (пример маркировки не дошёл), В-и (Огни залива).**
- **26.07b — «Собираем» №7 (Денис): build-trigger `9e2ecf5` =
  c18bfbb (фидбэк №1: окна/автоперекрытия/edge_offset). PDF Герману
  «обновление 26.07» (что было не так и почему, установка только
  AFrame, вопросы В-а…В-д, план развития 6 пунктов на согласование) —
  AFRAME_update_2607.pdf + генератор; отправлен Денису файлом.**
- **26.07a — ФИДБЭК ГЕРМАНА №1 ПО ATFRAME (скрины + разбор Дениса;
  сборка встала, знаки/слои живые) — ТРИ ЗАМЕЧАНИЯ ЗАКРЫТЫ.**
  (1) Стойки/кронштейны СКВОЗЬ ОКНА: C# пропускал полилинии БЕЗ метки
  ATCLAD (метка только на внешнем контуре!) → движок не знал о
  проёмах. Фикс: замкнутые полилинии выборки — геометрия ВСЕГДА
  (группировка по вложенности в движке), метки нужны только для осей;
  «ни одной метки» — ранний отказ с подсказкой. (2) Направляющие
  «бесконечной длины»: Герман не кликал отметки. Фикс: Enter без
  точек → вопрос «высота этажа (0 — без)» дефолт 3000 → floor_step в
  движок → автоперекрытия от низа контура (note) + note «направляющая
  длиннее хлыста 6000». (3) НОРМАТИВ «крепёж не ближе 100 мм от края
  проёма — бетон колется»: новый параметр системы edge_offset=100;
  ось ближе 100 к грани окна → на высоте окна кусок стойки СМЕЩЁН на
  ось «грань∓100», кронштейны и кляммеры куска — на смещённой оси;
  под/над окном — по оси руста (куски (lo,hi,x_eff) в движке).
  Тесты: 22→28 (FR7 смещение 996→900 с кронштейнами 2300/2900, FR8
  автоперекрытия 3000/6000, FR9 note хлыста) + 5 CLI
  (floor_step-проброс). mcs OK, дубли чисто. ОТКРЫТО (В22, к
  следующему прогону): стартовый кляммер на низе СМЕЩЁННОГО куска
  (сейчас ставится — как «начало куска»); точная граница смещённого
  куска = bbox окна (руст по низу/верху проёма); привязка стыка
  направляющих к отметке (центр, зазор 10). Ждёт «Собираем».
- **24.07-2 (Э2, эта сессия): КОМАНДА ATFRAME НАПИСАНА + БАНДЛ 5-м в
  CI.** FrameCommand.cs (самодостаточный, паттерн CladCommand, Run в
  try/catch→RunCore сразу): выбор зон с меткой ATCLAD (штриховки/
  марки/контуры; joints_x/rows_y ИЗ МЕТКИ — метка старой сборки без
  осей → предупреждение «перегенерируйте ATCLAD»), геометрия из
  fzones.json/полилиний, система кейвордами (классический
  конструктор, дефолт по не-OK — грабля 18.07r: Keywords.Default
  РОНЯЕТ компиляцию, прецедент TableCommand), подтверждение шагов
  [Принять/Изменить → 5 doubles], отметки перекрытий циклом точек,
  движок frame_engine.exe, вставка: направляющие = Polyline-
  прямоугольники (ширина rail_width системы), кронштейны/кляммеры =
  блоки-знаки на слоях Германа _01_ПС_*, метка ATFRAME + перегенерация
  (Erase любых Entity по handles). Знаки создаются кодом
  (EnsureSignBlock/EnsureClampBlock, сущности на слое «0»):
  НЕСУЩИЙ = ромб (квадрат 150 на 45°) с прямым крестом в круге r75
  («реже = несущий» — логика Дениса, частоты эталона 13/6/2),
  РЯДОВОЙ = квадрат с X-крестом в круге; кляммеры — прямоугольник
  100×12 (старт — с ножкой 40 вниз). ⚠ НОВЫЕ API без прецедента
  (BlockTableRecord/Polyline.AddVertexAt/Circle ctor) — компиляцию
  проверит check, рантайм знаков = первый прогон Германа. ATTDEF в
  знаках НЕТ (сознательно, меньше новых API) — добавить при
  подключении ведомости ATableSpec. МОСТ ИЗ ACLAD ГОТОВ:
  cladding_plan/clad_engine возвращают joints_x[] (стыки столбцов +
  грани проёмов ± полруста + юзер-русты) и rows_y[] (центры
  горизонтальных швов), CladCommand пишет их в метку ATCLAD (C34/
  C34b, 51+12 зелёные). frame_engine.py — CLI (op=frame, зоны
  facade_zone/1 + голые контуры, per_zone; 4 юнита). systems.json:
  + rail_width (60/40/60); поиск рядом с exe (PyInstaller onefile:
  __file__ в temp — искать sys.executable/_MEIPASS). build.yml: 5-й
  бандл AFrame.bundle.zip (dll+exe+systems.json); check.yml:
  компиляция AFramePlugin + юниты. mcs --parse OK, скан дублей чист,
  аудит хелперов OK. НЕ выкладывалось — ждёт «Собираем» и рантайма.
- **24.07 (Э1, эта сессия):** модуль создан; движок+справочник+22
  юнита зелёные; эталон подсистемы снят с боевого DWG Ленпроспекта
  aspose-cad'ом НАПРЯМУЮ (конверсия в DXF отвергнута — R12/2ГБ/блоки
  взорваны; скрипт-паттерн в журнале сессии, tools/dwg2dxf.py для
  простых случаев); конвенция Германа (слои/знаки/шаги) — в
  STAGE3_FRAME.md §Конвенция. check.yml дополнен тестами AFrame.
