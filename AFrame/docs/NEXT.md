# AFrame — состояние и хэндофф (ЧИТАТЬ ПЕРВЫМ)

> **Это проект AFrame — подсистема НВФ (этап 3 фасадного направления):
> кронштейны + направляющие + кляммеры по согласованной раскладке
> AClad. НЕ путать с LD/физикой.** Концепт и решения —
> `Facades/docs/STAGE3_FRAME.md` (В13–В20 закрыты Денисом/фактами
> боевого DWG 24.07). Грабли среды/сборки — `ATableSpec/docs/NEXT.md`;
> методика фидбэка — `ABlockGen/docs/NEXT.md`. Ветка общая
> `feat/auto-reactor`; `main` — только по «Пушим».

## TL;DR
- **Что:** команда **ATFRAME** (этап Э2, ещё не написана) — монтажные
  схемы подсистемы по зонам с меткой ATCLAD: стойки по осям
  вертикальных рустов, несущие кронштейны на отметках перекрытий
  (центр — В15), рядовые равномерно (В16), кляммеры по горизонтальным
  швам; условные знаки СВОИ (В13: DWG блоков Герман даст позже),
  слои Германа `_01_ПС_*` (В19).
- **Порядок целевой (Денис 24.07): расчёт несущей способности → шаги
  → расстановка.** Пока расчёта нет — шаги из справочника систем
  (systems.json: Вектор-1 / Standart / Межэтажная) с подтверждением
  конструктором; модуль расчёта (этап 4) встанет ПЕРЕД движком и
  заменит дефолты.
- **Статус (24.07, Э1 ГОТОВ):** движок `engine/frame_plan.py`
  (op=frame: стойки рвутся проёмами по bbox, сегменты режутся
  отметками перекрытий, стык направляющих центрован на отметке с
  зазором системы; несущие/рядовые/кляммеры; summary с м.п. и
  прикидкой хлыстов) + `systems.json` + **22 юнита** (FR1–FR6 —
  числа руками; FR-D — факты боевого эталона
  `atspec-testdata/dxf/facades/frame_lenprospekt/frame_ps.json`:
  кляммеры 605, кронштейн ~300+~950, стойки 602.5–605). C#/бандла НЕТ.

## Файлы
- `engine/frame_plan.py` — движок Э1 (докстринг = модель данных);
- `engine/frame_calc.py` — **движок этапа 4** (расчёт несущей
  способности по методике «Вектор фасад», 13 боевых статрасчётов;
  конспект — `atspec-testdata/docs/facades_calc/METHOD_CALC.md`);
- `engine/test_frame_calc.py` — 71 проверка (эталоны = числа их PDF);
- `engine/systems.json` — справочник систем (правила = атрибуты
  системы, вывод из дельты КАФТ↔Вектор);
- `engine/test_frame_plan.py` — 42 проверки (`PYTHONUTF8=1`, из
  каталога engine; FR-D скипается без testdata).

## Дальше (порядок ценности)
1. **Э2 — команда ATFRAME + бандл AFrame 5-м в build.yml**: чтение
   метки ATCLAD (оси из кромок камней; в метку AClad дописать
   joints_x[]/rows_y[] при ближайшей правке), диалог (система,
   отметки перекрытий кликами, подтверждение шагов), блоки-знаки в
   бандле (квадрат/ромб с крестом в круге r75 — по образу Германа,
   какой знак несущий — спросить), вставки на слои `_01_ПС_*`, метка
   «ATFRAME», перегенерация, clad_engine-паттерн CLI (frame_engine.py).
2. Прогон Германа на его фасаде → фидбэк → правки; его ручной эталон
   подсистемы на нашем тестовом фасаде → полная D-сверка.
3. Ведомость подсистемы: МАРКИРОВКА у знаков ✓ 01.08a (ATSPECREPORT
   по слоям `_01_ПС_*`, Count по МАРКИРОВКА — кода в ATableSpec не
   нужно); ЗАХВАТКА отложена (Денис 01.08), профили — после блоков
   Германа.
4. Этап 4 — модуль расчёта (методики от Дениса) → шаги считаются.

## Открытое
- **В-н (этап 4):** floor-правило констант неразрезной балки и
  консервативная трактовка угловой Новгорода — подтвердить у Германа
  (их эксель местами непоследователен: рядовая 2-пролётные, угловая
  3-пролётные при одинаковой геометрии).
- **В-о (этап 4):** конструктивные MAX шага кронштейнов по системам
  (расчёт монолита пропускает и 1000+, но их вывод 800 — предел из
  АТР?): ГП-40-40→800? ШП-90-20→1200? — таблица от Германа.
- **В-п (этап 4):** высотные диапазоны лестницы шагов (0–24/24–45/
  45–65 у Нижнекаменской) — фиксированные пороги или от проекта?
- Какой из двух знаков Германа = несущий (квадрат с X-крестом или
  ромб с прямым крестом) — спросить при первом прогоне.
- **В-ц (30.07, НОВЫЙ):** в МЕЖЭТАЖНОЙ стойка у окна идёт по центру
  руста БЕЗ смещения (ТЗ §2), ближайшая ось может быть в 300–500 мм от
  откоса — на неё ставить боковой кляммер или у окна нужна отдельная
  стойка ближе к грани? Сейчас консервативно: боковой только если ось
  ближе `WIN_NEAR`=200 мм, иначе рядовой.
- **В-ч (30.07):** минимальная высота ряда плитки — порог слияния
  кляммеров `CLAMP_MERGE`=100 мм взят с потолка; если бывают полосы
  подрезки ниже 100 мм со своим кляммером, порог менять.
- **В-ш (30.07): ЗАКРЫТ письмом Германа 01.08.** Допустимые:
  вертикальная ГП-40-40/ГП-60-40/ШП-60-20; межэтажная НСП-1/НСП-2
  (верт.), НГП/СП-60-40 (гориз.); ортогональная ШП-60-20/ZП-40-20
  (верт.), ГП-40-40/ГП-60-40 (гориз.). Подбор ограничен
  (`_VERT_ALLOWED` по scheme, FR-P6).
- **В-я (02.08):** чем НСП-1 отличается от НСП-2 и когда какой
  (в диалоге межэтажной пока ручной выбор, дефолт НСП-1); и нужны
  характеристики сечения ГП-60-40 (Wx/Jx/A/q) — в расчётном
  справочнике его нет, при явном выборе считаем по ГП-40-40 в запас.
- **В-аа ЗАКРЫТ (ответ Германа 07.08, реализовано 07.08b):**
  «Угловые вдоль оконных боковых откосов и боковых границ замкнутой
  области (внешний/внутренний угол); плитка шире 600 — боковой по
  середине плитки; установка бокового ВЕРТИКАЛЬНО; также боковые под
  отливом и последним рядом по высоте». Совпало с правилом, выведенным
  из полигона (07.08a, `engine/analiz_klu_poligon.py`: 202+80+27=309
  КЛУ орто точно). Реализация: `_piece_clamps(on_seam)` — рядовой на
  оси-русте, боковой orient="v" на оси в поле плиты (mid/угловые/
  краевые) и оконных; над стыком вне руста ряд ПУСТ (полигон);
  `_edge_top_clamps` — боковой orient="h" под отливом (верх куска =
  низ проёма в створе) и последним рядом (верх зоны). C#: боковые
  v вставляются с поворотом 270° (центрирование базы уходит в Y).
  Остаточные расхождения с полигоном (не блокируют): его сквозные
  профили на швах подрезки у откосов дают столбики по всей высоте
  (у нас куски по ТЗ «сторона окна + 100»); пары КЛУ с шагом 188 у
  низов проёмов на рядовой — спросить при случае.
- **В-аб (02.08, полигон):** межэтажная — верхний ряд кронштейнов в
  +300 над последним перекрытием (у нас нет); столбец кронштейнов с
  шагом 3000 на ортогональном участке (x=48851) — что это; гориз.
  шаг межэтажной 449, у краёв 225 (уплотнение угловой зоны?).
- **В-аг (03.08, аудит):** ось руста в 50..300 мм от грани окна —
  ставить ли ВСЁ РАВНО отдельную оконную стойку в 100 от грани
  (буква ТЗ) или ближняя ось её заменяет? Сейчас: ровно совпавшая
  (±50) — заменяет, дальше — ставим стойку.
- **В-ав (03.08, полигон):** метизы межэтажной — у Германа на узле
  стыка НСП×НГП стоят ВС-300 и В-70 ПАРОЙ (по 95 шт), у нас один
  знак «вставка» на узел. Уточнить состав узла (для ведомости это
  ×2 позиции) и роль «6 - Перемычки для доп.кляммеров» (×1 на
  полигоне).
- **В-ад ЗАКРЫТ (ответ Германа 07.08, реализовано 07.08b): ОБЕ
  схемы верны.** Точки перекрытий УКАЗАНЫ → наш сценарий: стык
  направляющих на отметке, несущий кронштейн на центре перекрытия,
  дальше опорные (В15). НЕ указаны → его сценарий («на полигоне
  везде монолит, все кронштейны одного типа»): ХЛЫСТЫ rail_std от
  низа зоны с зазором МЕЖДУ (низы 24070.3+3010n), кронштейны «300
  от торцов + равномерно ≤ шага», несущих нет; короткий обрезок —
  один кронштейн по центру. Реализация: `lash = not floors` в
  вертикальной ветке; автогенерация виртуальных отметок из
  floor_step оставлена ТОЛЬКО межэтажной; C#-диалог спрашивает
  высоту этажа только у межэтажной. D-сверка рядовой: кронштейны
  628/611, СОВПАВШИХ ПОЗИЦИЙ 133 (было 10). В-ае(1) ЗАКРЫТ тем же
  ответом: на полигоне ATFRAME ЗАПУСКАЛСЯ (п.4) — атрибуция
  смешанная, кляммеры при этом ручные (наш слой пуст).
- **В-ае (04.08): (1) ЗАКРЫТ 07.08 — «на полигоне запускалась
  команда ATFRAME»** (атрибуция смешанная; кляммеры при этом ручные
  — наш слой кляммеров пуст). (2) ОТКРЫТ: правило горизонтального
  шага колонок орто ~785.6 (наш движок делит под ≤800 → 748, 22
  колонки против его 35) — спросить при случае.
- **В-аж ЗАКРЫТ (ответ Германа 07.08, п.3/п.5): в ведомость считать
  ВСЁ ВМЕСТЕ, не разделяя.** Требуется только проверка задвоений:
  элементы с полностью совпадающими координатами → оставить один.
  Реализовано 07.08b: команда **ATDEDUP** (AFrame): область рамкой
  (Enter — весь чертёж), вхождения блоков сравниваются по
  (определение, слой, X, Y, поворот, масштаб) с допуском 0.1 мм/0.1°,
  лишние удаляются, отчёт по именам в командную строку, откат — U.
  Живой кейс: на самом полигоне 84 задвоенных направляющих
  (`_01_ПС_НАПРАВЛЯЮЩИЕ`: 614 вставок, 530 уникальных позиций).
- **В-щ (30.07):** масса кронштейнового узла для критерия выбора пары
  «профиль+шаг» (`M_BRACKET`=0.5 кг — оценка).
- **В-э (30.07):** «уголок крепления пропал» — узел с крестиками в
  овале у низа стоек НА ЧЕРТЕЖЕ ЕСТЬ (видео2 1:55/2:01), Герман
  покажет конкретное место. Плюс: что за отдельный фрагмент раскладки
  6×3 слева от фасада (видео1 6:32) и что не так с ATCLADDIM.
- **Блокер по блокам профилей: ЗАКРЫТ письмом 01.08** (имена
  состояний видимости = марки профилей) и реализацией 02.08:
  `InsertRailBlock` ставит видимость по `rails[].profile`
  (нормализованный матч по AllowedValues). Проверка вживую — на
  прогоне Германа: точных строк состояний мы не видели (aspose
  динамику не разбирает), матчер терпим к регистру/дефисам/Z↔З.
- **В-ю (01.08): ЗАКРЫТ ответом Дениса 01.08 (тем же днём).**
  Захватка = зона этапа 1: участок, который проектировщик обвёл и
  обсчитал в ATFZONE (контур минус проёмы); объём работ одного
  участка — «пока техника не переехала на другой фасад»; понятие
  человеческое, программе достаточно совпадения с зоной. Реализовано
  01.08b: ЗАХВАТКА = zone_id этапа 1 (тот же id, что в сводной
  таблице ATFTABLE; голый контур — хэндл полилинии) в знаках AFrame
  И в кассетах AClad.
- Половинки кляммеров на краях/примыканиях — Э2+ (сейчас не ставятся).
- Угловые спецузлы (крест-накрест, короба) — сознательно НЕ моделируем
  (В17: типовой случай, угловая зона = полоса шириной ~плитка).

## Статус
- **07.08b — ОТВЕТЫ ГЕРМАНА: В-аа/В-ад/В-ае(1)/В-аж/В-аз ЗАКРЫТЫ,
  всё реализовано.** Движок: (1) хлыстовый режим вертикальной при
  пустых floors_y (В-ад: «обе схемы верные» — с отметками несущий на
  центре перекрытия, без отметок хлысты снизу вверх, все кронштейны
  одного типа; автогенерация отметок из floor_step оставлена только
  межэтажной); (2) типы кляммеров по положению оси (В-аа: рядовой на
  русте, боковой orient="v" в поле плиты/у окна, над стыком вне руста
  пусто) + `_edge_top_clamps` (боковой orient="h" под отливом и
  последним рядом по высоте) — во всех трёх ветках; (3) угловая зона
  и стойки только ВНУТРЬ (В-аз). Юниты: FR8 переписан (хлысты), FR9
  (note от floors), FR-H3 (orient v/h), K1–K6b новые; frame_plan 128,
  frame_engine 8 (+межэтажная), аудит 154 — зелёные. D-сверка рядовой:
  кронштейны 628/611 (Δ+2.8%), совпавших позиций 133 (было 10);
  орто кляммеры 626/737 (было 566, разрыв −15% — его сквозные
  столбики у откосов, у нас куски по ТЗ). C#: диалог отметок (высота
  этажа — только межэтажной, иначе сообщение про хлысты); боковые
  v-кляммеры вставляются с поворотом 270° (центрирование базы в Y);
  команда **ATDEDUP** (п.5 ответа: дубли по координатам → оставить
  один; на полигоне таких 84); выбор слоя из списка существующих
  ВЕЗДЕ (просьба Германа): ATCLAD — форма `LayerPickForm`
  (наименование облицовки), ATFZONE — слои чертежа в комбо
  наименования; ATCLADDIM/ATFRAMEDIM уже были (04.08g). Манифест
  AFrame: команды ATFRAMEDIM/ATDEDUP дописаны.
- **07.08a — В-аа и В-ад РАЗОБРАНЫ ДО ПРАВИЛ по полигону (сборка №17
  зелёная; правила подтверждены ответами Германа тем же днём).** Скрипт `engine/analiz_klu_poligon.py` (числа
  воспроизводимы; докстринг = полная сводка). Главное:
  (1) **В-аа:** тип кляммера = положение оси относительно плит; на
  рядовой плиты КРУПНОФОРМАТ ЦЕЛЬНЫЕ (1122/1200/1072/896/843/608/314
  — по определениям *U-блоков, скрытых рустов нет). Рядовой — на
  швах; угловой rot=270 — ось в поле плиты/у откоса/край (ряды у
  откосов ПО ВСЕЙ ВЫСОТЕ); новые классы: держатели верхних кромок
  rot=0 (верх зоны 27, подоконные 80 на орто). 202+80+27=309 ✓.
  (2) **В-ад:** «пара −300/+310» — не правило стыка, а торцы ХЛЫСТОВ
  3000 (зазор 10, от низа зоны, перекрытия полигону неизвестны);
  кронштейны «300 от торцов + ≤800» = буква ТЗ 26.07. Оконные оси
  сквозные (стыки на низ проёма −50). (3) База вставок Германа:
  КЛР/КЛС −56 от оси, КОМБ по оси, КЛУ +13.9 (rot=270) — ещё одно
  подтверждение CenterOffsetX (04.08h). (4) MEZH-кляммеры ≈ копия
  ORTO (628/730 позиций совпали относительно низа-лева зоны).
  Вопросы Герману переписаны в В-аа/В-ад как подтверждения правил.
- **04.08f/g/h — ОСТАТОК ФИДБЭКА ЗАКРЫТ (Р-1..Р-4).**
  - **Р-3 (движок, 04.08f): угловые и краевые зоны стоек.** Причина
    «не дорисовывает профиль и кронштейны»: стойки ставились ТОЛЬКО по
    осям рустов, а у внешнего угла и конца облицовки рустов нет.
    `_zone_rail_axes`: от указанного угла зона `corner_zone`, первая
    стойка в 100 мм от угла, дальше шагом угловой зоны; в краевой зоне
    (край области без угла) последняя стойка в 100 мм от границы.
    Новые оси сливаются с рустами, совпавшие ближе 50 мм отбрасываются.
    Поля `rail_step_corner`/`rail_step_main`/`edge_rail_off`, включается
    наличием `corners_x` (в т.ч. ПУСТОГО списка) — без них поведение
    прежнее. Юниты A9..A10c, аудит +5 сценариев → 154.
  - **Р-1 (C#, 04.08g): область для размеров.** ATCLADDIM и ATFRAMEDIM
    сначала просят выбрать область рамкой (фильтр INSERT); ряд/столбец
    ищется только среди выбранных. Enter — прежнее поведение по всему
    чертежу, принятое печатается в командную строку.
  - **Р-2 (C#, 04.08g): выбор слоя списком.** `DimForm` — форма на
    WinForms, собрана кодом (без дизайнера и .resx): тип + слой из
    существующих со свободным вводом. Дефолты прежние; у подсистемы
    первым выбором «Столбец». В csproj добавлены System.Windows.Forms
    и System.Drawing (WPF-рефы оставлены — грабля-12).
  - **Р-3 (C#, 04.08g):** в ветке ручных шагов ATFRAME спрашивается шаг
    СТОЕК в угловой зоне (0 — только по рустам); сам факт ручного ввода
    включает `exact_step` — заданные 800 ставятся буквально (п.1).
  - **Р-4 (C#, 04.08h): кляммер центрируется по оси профиля.**
    Замечание «рядовые кляммеры со смещением от центра профиля»
    оказалось про БАЗУ его блоков: у «рядовой!КЛР-1-н» графика
    X 0..112.5 (центр 56.25), у «стартовый!КЛС-1-н» X 13.5..99.0, а у
    углового и комбинированного база в центре. На полигоне его вставки
    стоят на −56.3 от оси у рядовых и стартовых и на нуле у остальных —
    он центрирует знак руками. `CenterOffsetX` (по кривым определения)
    сдвигает вставку на −dx, только по X: по Y база «низ-центр» у
    профилей осмысленная. Кронштейнов не касается — их X совпадают с
    осями 39 из 39.
  **В-аз ЗАКРЫТ (ответ Германа 07.08, п.6): «угловая зона только во
  внутрь».** Реализовано 07.08b: `_corner_ivals` и `_zone_rail_axes`
  строят зону/стойки от угла ВНУТРЬ области (для угла в середине
  области направление — от ближайшего края; юниты K6/K6b).
- **04.08e — ФИДБЭК ГЕРМАНА ПО СБОРКЕ №16: ответы на вопросы + три
  фикса движков.**
  **ОТВЕТЫ ГЕРМАНА (закрывают вопросы):**
  - **В-я ЗАКРЫТ частично:** НСП-1 и НСП-2 отличаются СЕЧЕНИЕМ; в
    проектах бывают межэтажные профили разной толщины/сечения в
    зависимости от высоты здания и расстояния между перекрытиями —
    для их различения и введён НСП-2. **По умолчанию НСП-1** (у нас
    так и есть). Характеристики ГП-60-40 (Wx/Jx/A/q) — всё ещё нужны.
  - **В-аб ЗАКРЫТ:** верхний ряд кронштейнов в +300 на межэтажной —
    НЕ правило, а их предположение «область парапета», под неё
    рисовали подсистему. Отдельного правила в движок не вносим.
  - **В-аг ЗАКРЫТ:** ось руста БЛИЖЕ 100 мм от грани окна — заменяет
    отдельную оконную стойку; ДАЛЬШЕ 100 — отдельная стойка остаётся.
    Текущее поведение уже такое (side-механизм смещает ось при
    расстоянии < edge_offset, ровно-100 совпадает по координате и
    сводится дедупом 04.08b). Правок не потребовалось.
  - **В-ав ЗАКРЫТ:** узел НСП×НГП — в ведомость ДВЕ позиции, ВС-300 и
    В-70. «Перемычка для доп. кляммеров» — Герман её не нашёл, скорее
    всего ошибка в чертеже; в модель не вносим.
  - Про знак несущего: в его примере вертикальной ВСЕ кронштейны
    одинаковые — так бывает, когда основание целиком монолитное.
  **ФИКСЫ (движки, юниты зелёные):**
  - **AClad, ортогональность контура (главное).** Жалоба «сложные
    области не раскладываются, программа очень чувствительна к
    ортогональности». Корень: `_is_ortho` требовал точности 1e-6 мм —
    отклонение в сотую долю миллиметра роняло контур МОЛЧА. Сложные
    ортогональные формы (Г-образная, ступенчатая) раскладывались и
    раньше: падала неточность рёбер, обычная для контура из АР.
    Добавлен `_ortho_snap` (допуск `ortho_tol`, дефолт 5 мм, два
    прохода); реально наклонные рёбра отклоняются, но нота называет
    величину и допуск. Юниты C35–C38, всего 58.
  - **AFrame п.4:** `mid_rail_over` применялся только в вертикальной
    ветке, у Ортогональной стоял null → промежуточный ШП посередине
    пролёта шире 650 не ставился. Исправлено, юнит A8.
  - **AFrame п.1 «задал 800 — ставит 798»:** это работа В16
    (размазывание остатка: на куске 2990 между крайними 2390, три
    пролёта = 796.7). Ручной шаг теперь ставится буквально —
    `exact_step` (добор только последним пролётом), прежнее поведение
    по умолчанию. Юниты A7/A7b, поле в CLI base (грабля-13).
  **ОСТАЛОСЬ ИЗ ФИДБЭКА (следующий заход):**
  - **Р-1 (C#):** ATCLADDIM/ATFRAMEDIM ставят размеры по ВСЕМУ чертежу
    вместо заданной области — нужен выбор области (рамкой) перед
    указанием камня/кронштейна.
  - **Р-2 (C#):** выбор слоя из выпадающего списка существующих —
    для ATCLADDIM/ATFRAMEDIM и вообще для AClad/AFrame (нужна форма,
    в командной строке списка слоёв не сделать).
  - **Р-3 (движок+диалог, крупное):** угловые и краевые зоны AFrame.
    Сейчас профиль и кронштейны в них не дорисовываются. Должно быть:
    от указанной точки угла идёт зона шириной 1500; свои шаги для
    угловой и рядовой зон (запрашиваются); в угловой ПЕРВЫЙ
    вертикальный профиль в 100 мм от внешнего угла, дальше по шагу;
    в краевых зонах (внутренний угол здания или конец облицовки)
    ПОСЛЕДНИЙ профиль в 100 мм от границы, остальное как в рядовой.
    NB: `_corner_ivals` сейчас строит полосу czone в ОБЕ стороны от
    угла — по формулировке Германа зона идёт в одну сторону, уточнить.
  - **Р-4 (уточнить):** «рядовые кляммеры рисуются так же со
    смещением от центра профиля» — не ясно, что именно не так;
    вопрос Герману с картинкой.
- **04.08b — D-СВЕРКА ПОЛИГОНА (задача 4 очереди): найден и закрыт
  дефект наложения стоек; атрибуция полигона ИСПРАВЛЕНА.**
  Скрипт `engine/dsverka_poligon.py` (руками, как audit_*), генератор
  дампа `tools/dwg_probe_tree.py`. Грабля: `ci.entities` у aspose даёт
  99 сущностей из 6225 — геометрия в блоке `*Model_Space`.
  **ДЕФЕКТ (закрыт):** ось руста ровно в `edge_offset` (100) от грани
  проёма — она же цель смещения СОСЕДНЕЙ оси, стоящей ближе грани (на
  полигоне такие пары идут с шагом 96, 16 осей из 39). Смещённый
  оконный кусок ложился ПОВЕРХ собственной оси: два профиля в одном
  месте, **64 дубля кронштейнов**, двойной счёт в ведомости. Порядком
  обхода не лечится — схлопывание бывает с ОБЕИХ сторон окна. Решение:
  укладка кусков вынесена за цикл по осям, `_dedup_pieces` сводит их по
  X (приоритет — собственная ось; от смещённого остаётся незанятая
  часть; осколки <100 не ставим). Эффект: рядовая 692 → 640 при его
  611 (было +13.3%, стало +4.7%). Юниты **107** (+FR-A5 ×3 стороны,
  +FR-A6), в аудите чекер **R4b «дубль кронштейна»** (был только для
  кляммеров) и 8 сценариев «пара близких осей у грани» → **149
  сценариев, 0 нарушений**; проверено откатом (без фикса FR-A5
  краснеет, аудит даёт 32 наложения). calc 79, engine 8, clad
  51+12+124 — зелёные.
  **АТРИБУЦИЯ ПОЛИГОНА — ПРЕЖНИЙ ВЫВОД БЫЛ НЕВЕРЕН.** Слои `_01_ПС_*`
  — конвенция ГЕРМАНА (снята с Ленпроспекта 24.07, STAGE3_FRAME.md
  «наши слои этапа 3 — ЭТИ»), наш ATFRAME пишет в НИХ ЖЕ → по слою
  авторство не различить. Достоверно наши в полигоне — только 122
  блока `AFRAME_*` (вставка/скоба межэтажной; кронштейны и
  направляющие ставятся ОБРАЗЦОМ и носят имена Германа). Кляммеров на
  `_01_ПС_КЛЯММЕРЫ` в файле **0** при 566..790 у движка → полного
  прогона ATFRAME там не было; подсистема полигона, судя по всему,
  РУЧНАЯ, а совпадения — совпадения ПРАВИЛА (В-ае).
  **СОВПАЛО** (в FR-D2 после подтверждения авторства): орто —
  Y-уровни `24370.3 + 600n` (старт = низ + 300, шаг 600) точно; орто —
  доп. кронштейны у граней проёмов ровно в 100 (наш п.9), 55 колонок
  его чертежа; рядовая — X кронштейнов ⊆ X направляющих 39/39;
  рядовая — кляммеры 790 против 791.
  **РАЗОШЛОСЬ** (в вопросы, по догадке НЕ чиню — грабля-17): рядовая —
  его цепочка `24370.3 + 800×15`, у стыка ПАРА −300/+310 (300 от торца
  куска при зазоре 10), кронштейна на самой отметке НЕТ, у нас несущий
  по центру отметки (В-ад); орто — гориз. шаг колонок ~785.6, у нас
  748, колонок 35 против 22 (В-ае); орто — кляммеры 566 против 737,
  недобор ≈171 сидит в угловых КЛУ-1.1 (В-аа); орто — направляющие 180
  против 347 (его `!3_` идут rot=0, наши rot=270).
- **03.08b/c/d — КРАШ-АУДИТ ПО ЗАКАЗУ ДЕНИСА («найти всё до
  Германа»).** Метод: чекеры геометрических инвариантов выхода
  (стойка не сквозь окно; кронштейн/кляммер не в воздухе; наложения
  кусков; шаги/отступы; дубли) + 141 сценарий frame_plan (7 типовых
  × 3 системы + 120 fuzz) + 124 сценария cladding_plan + 270
  комбинаций frame_calc (+монотонность по весу/высоте/выносу/анкеру,
  отказы) + fuzz площадей facade_zones против независимого shoelace
  + рендеры глазами + C#-скан граблей. Скрипты СОХРАНЕНЫ:
  `AFrame/engine/audit_frame.py`, `AClad/engine/audit_clad.py`
  (запуск руками, не юниты). НАЙДЕНО И ЗАКРЫТО 4 дефекта движка:
  (1) стойка/Z СКВОЗЬ СОСЕДНЕЕ окно — смещённые куски и
  гарантированные не резались чужими проёмами; (2) верхние доп.
  кронштейны п.9 (орто) В ВОЗДУХЕ — добавлена перемычка ГП-40-40 над
  окном между осями Z; (3) ДУБЛЬ стойки при русте ровно в edge_off
  от грани (строгое «<» side-механизма); (4) дубль при
  ПЕРЕСЕКАЮЩИХСЯ окнах. Решение: точное вычитание (чужие окна +
  существующие куски оси ±50), осколки <100 не ставятся. Чисто:
  кляммеры, шаги, calc, AClad (пересечения/выходы/min_cut), zones
  (площади), C# (локали/кейворды/LockDocument/MaxJsonLength).
  Юниты: frame_plan 103 (+FR-A1..A4), calc 79, engine 8, clad 51+12,
  facades 35+22 — зелёные. Метизы полигона: у Германа на узел идут
  ВС-300 и В-70 ПАРОЙ (95+95) — у нас один знак «вставка» (В-ав).
- **03.08a — скрин Германа «Ошибка при загрузке элемента меню:
  ACAD:Фасады» — ПРИЧИНА НАЙДЕНА, ФИКС (войдёт в №14).** COM-меню
  «Фасады» вставлялось в menubar → AutoCAD запоминал «ACAD:Фасады» в
  ПРОФИЛЕ → на старте профиль восстанавливает menubar РАНЬШЕ
  загрузки наших DLL → ошибка (косметическая: меню затем
  пересоздавалось и работало). Фикс ×3 FacadesClassic (AFrame/AClad/
  AFacades; в боевом AFacades тронут ТОЛЬКО общий UI-файл — это
  замечание Германа): BeginQuit → Cleanup (каждая DLL снимает свои
  пункты, последняя видит пустое меню → RemoveFromMenuBar → профиль
  чист), QuitAborted → Init (отмена выхода возвращает меню).
  ATFRAMEDIM добавлен в классику («Размеры кронштейнов») и ленту
  AFrame («Размеры»). mcs --parse ×3 чист. BeginQuit/QuitAborted —
  новые для репо события managed API: компиляцию подтвердит CI
  check, поведение — рантайм Германа. Строки «*Прервано*» и
  TrustedDWG на его скрине — не наши: прерванная загрузка меню-
  утилит (Esc) и штатное уведомление формата.
- **02.08b/c — ПИСЬМО ГЕРМАНА 01.08 РЕАЛИЗОВАНО (профили-видимость +
  ATFRAMEDIM) + ПОЛИГОН-ЭТАЛОН РАЗОБРАН + закрыто замечание №1
  (Z/стойки у окон всегда).**
  Полигон: `atspec-testdata/dxf/facades/poligon_0208/` (DWG + дамп
  6225 вставок; читан aspose-cad напрямую, рендер+scatter). Факты:
  сетка ortho 600 по вертикали ЕГО РУКОЙ (26 шт в столбце — пересмотр
  №1 закреплён); межэтажная — кронштейны на перекрытиях каждые 3000,
  гориз. шаг 449/225 у краёв, верхний ряд +300 (В-аб); кляммеры:
  УГЛОВЫЕ 60% парами вдоль вертикальных рустов по всему полю (В-аа —
  расходится с нашей моделью «боковой только у окон», НЕ чиню без
  правила — ГРАБЛЯ-17); наша генерация лежит рядом с ручной →
  будущая D-сверка.
  Движок (98 юнитов, +FR-P1..P6b, FR13 обновлён): `rails[].profile` =
  марка для видимости (выбор пользователя → подбор → пресет);
  `nsp_type` НСП-1/НСП-2; Z→«ZП-40-20»; В-ш: `_VERT_ALLOWED` по
  scheme; явный ГП-60-40 → расчёт по ГП-40-40 в запас + note;
  ЗАМЕЧАНИЕ №1 ЗАКРЫТО ПО-НАСТОЯЩЕМУ: оконные стойки/Z у КАЖДОЙ
  грани проёма ВСЕГДА (раньше Z возникал только если ось сетки
  случайно в edge_off СНАРУЖИ грани: в боевых фикстурах Z=0, доп.
  кронштейны п.9 висели без профиля; вертикальная — окно между осями
  оставалось без стоек и боковых кляммеров; межэтажная не тронута —
  В-ц). CLI: rail_profile/nsp_type в base (ГРАБЛЯ-13).
  C# (mcs --parse чист): диалог профиля (вертикальная
  [Авто/ГП40/ГП60/ШП60], межэтажная [НСП1/НСП2]); `InsertRailBlock`
  СНАЧАЛА видимость (свойство ищется ПО ЗНАЧЕНИЯМ — AllowedValues
  содержит марку; матч нормализованный `Norm`: регистр/дефисы/
  лат→кир вкл. Z→З; точное > кратчайший префикс, чтобы ШП-60-20 не
  взял ШП-60-20-20), ПОТОМ растяжка; **ATFRAMEDIM** — размеры между
  центрами кронштейнов столбца/ряда (полоса ±100 от кликнутого, то
  же определение+слой, цепочка RotatedDimension на `_РАЗМЕРЫ_ПС`,
  задвоенные знаки <1 мм пропускаются).
- **01.08c — PDF-дополнение Герману + «Собираем» №12.**
  `AFRAME_addendum_0108.pdf` (генератор `make_addendum_0108.py`):
  что нового 01.08a/b, пошаговая ведомость подсистемы через
  ATSPECREPORT (источник = слой `_01_ПС_*`, Марка =
  Object.«МАРКИРОВКА» с группой, Кол-во = Count, фильтр по
  ЗАХВАТКА; захватка = зона ATFTABLE), мини-тест-план из 3 пунктов,
  установка (замена AFrame.bundle + AClad.bundle). Инструкция 30.07
  остаётся в силе — это дополнение. Сборка №12 запущена этим же
  заходом (build-trigger по ГРАБЛЕ-4/5).
- **01.08b — ЗАХВАТКА в знаках и кассетах (В-ю закрыт; задача 5
  ДОДЕЛАНА).** Денис дал семантику: захватка = зона этапа 1 (участок,
  обведённый/обсчитанный в ATFZONE; неформализуемое человеческое
  понятие «объём работ, пока техника на участке» — программе
  достаточно тождества с зоной). ЗАХВАТКА = root из цепочки
  ATFZONE→ATCLAD→ATFRAME (= строка сводной таблицы ATFTABLE →
  ведомость подсистемы по захваткам бьётся с площадями; голый контур
  — хэндл). AFrame: второй невидимый ATTDEF в EnsureMarkDef, FillAttrs
  (mark, zahv), RootOf; AClad: в attref-цикле кассет заполняется
  ЗАХВАТКА (было: только копия дефолтов; МАРКИРОВКА камня — В10,
  ждём Германа). mcs --parse оба модуля чист; движки не тронуты.
- **01.08a — СБОРКА №11 ЗЕЛЁНАЯ (подтвердил Денис); МАРКИРОВКА в
  знаках подсистемы (задача 5, первая половина).** Знаки — кронштейны,
  кляммеры, вставки/скобы — получили невидимый ATTDEF «МАРКИРОВКА»
  (вид знаков не меняется), значение = вид элемента: «кронштейн
  несущий/рядовой», «кляммер стартовый/рядовой/боковой/
  комбинированный», «вставка», «скоба С1». Механика: `FillAttrs`
  копирует ATTDEF→ATTRIB (паттерн ABlockGen/AClad «из текущего
  представления»), `EnsureMarkDef` домешивает атрибут в НАШИ
  определения (в т.ч. в старый чертёж); определения ОБРАЗЦОВ Германа
  НЕ правим — у них заполняется только уже имеющаяся МАРКИРОВКА, у
  образцов без неё ведомость идёт по Object.Name (ATableSpec умеет).
  Побочно закрыт латентный баг: вставки образцов с атрибутами раньше
  создавались ВООБЩЕ без ATTRIB'ов. ЗАХВАТКА ОТЛОЖЕНА (решение Дениса
  01.08, до фидбэка Германа; встанет одной строкой в FillAttrs).
  Профили (динблоки Германа / прямоугольники-полилинии) маркировку
  пока не несут — вместе с блокером по блокам профилей. mcs --parse
  чист, рефы csproj не менялись, движок не тронут (91/79/8 зелёные).
- **30.07a–e — ФИДБЭК ГЕРМАНА №3 (видео 29.07 ×2 + письменные ответы
  30.07) РАЗОБРАН И ЗАКРЫТ КОДОМ.** Материалы: два mp4 (432 + 212 с),
  раскадровка ffmpeg 1/5 c + прицельные зумы; шкала СКВОЗНАЯ по двум
  файлам (транскрипт 0:00–10:40 = видео1 + видео2). Фонетика правлена
  по кадрам: «ледовая система» → **рядовая**, «клявер/километр/
  Жектамеры/богу пламя» → **кляммер**, «кодовать» → копировать,
  «размеры обрисовки» → облицовки.
  **ДВА ПЕРЕСМОТРА СОБСТВЕННОГО ВЫВОДА (важно, не откатывать):**
  (1) **5 кронштейнов на 3 м в ортогональной — НОРМА**, а не
  избыточность: по ответу п.11 (профиль 3000, свес ≤300, стык
  посередине между кронштейнами) сетка 600 по высоте даёт 300/900/
  1500/2100/2700 и стык на 3000 ровно между 2700 и 3300. Жалоба с
  видео относилась к ВЕРТИКАЛЬНОЙ. Шаг 600 в ortho НЕ трогать.
  (2) **Подбор профиля шаг НЕ увеличивает — режет АНКЕР.** Проверено:
  180 кг/м², 57 м, район IV → анкер 464 кг при допустимых 306, у
  профиля запас 11× и прогиб 0.1 из 5.3 мм; НСП-95-70 и ЗП-40-20 дают
  ОДИН шаг; при na_max 9000 Н шаг 800 вместо 500. Отсюда
  `binding_check` и печать ограничивающего узла в F2.
  **СДЕЛАНО (движок):** п.5 — `_merge_clamps` (кляммеры ближе
  CLAMP_MERGE=100 сливаются, приоритет боковой>стартовый>
  комбинированный>рядовой); причина задвоения найдена прогоном —
  верх откоса чуть НИЖЕ ближайшего шва раскладки (3600 vs 3630 → 30
  мм между знаками), НЕ «стартовый на каждом куске» (первая гипотеза
  БЫЛА НЕВЕРНА, отбита юнитом FR3 дважды). п.4 — боковой у грани
  проёма во ВСЕХ системах и только в высоту окна (+выступ 50 у отлива);
  в межэтажной `side` был захардкожен False. п.3 — кусок ≤ `rail_std`
  (3000) не режется отметкой перекрытия. п.11 — `_cut_by_len`: ortho
  режет ШП по 3000, стыки на кратных 3000 (зазор центрован, НЕ
  накапливается). п.9 — доп. кронштейны у граней проёма (сетка дальше
  `ortho_oh` → доп. на `edge_rail`=100; сбоку в высоту окна, сверху в
  ширину). п.10 — краевой/угловой межэтажный профиль в 100 мм от края
  зоны (обновлены эталоны FR4c/d + FR11e). п.7 — СП-60-40 к БЛИЖАЙШИМ
  осям (ось НА грани отбрасывалась строгим `<`). п.2 —
  `pick_profile_step` (перебор PROFILES, критерий q/b + M_BRACKET/(b·шаг),
  M_BRACKET=0.5 кг) + `binding_check`; `auto_profile` дефолт ON,
  `profile`/`binding`/`variants` в calc_report.
  **C#:** PrintCalcReport печатает подобранный профиль и
  ограничивающий узел; п.12 — `ScaleFactors = 1:1` в InsertSigns.
  **Тесты: frame_plan 91 (+FR-H1..H7), frame_calc 79 (+FC-H1..H3),
  engine 8, clad 51+12, facades 57 — зелёные.** mcs --parse ×3 OK,
  скан дублей чист (повторы `zone_id/system/floors_y/row` — ложные,
  это массивы строк, не инициализаторы).
  **Герману отдан** `docs/AFRAME_feedback_3007.pdf` (генератор
  `make_feedback_3007.py`): что исправлено, как интерпретированы его
  ответы, ОБА пересмотра, открытые вопросы, установка, тест-план из 8
  пунктов. **СБОРКИ НЕ БЫЛО — ждёт «Собираем» (следующая №11).**
- **29.07a — СБОРКА №10 ЗЕЛЁНАЯ (build-bundle #83, `66a2728`, 5
  артефактов — скрин Дениса; красный check aa78fe8 был логом ДО
  фикса Microsoft.CSharp, диагноз подтверждён: CS0656 в
  FacadesClassic). Герману отдан ПОЛНЫЙ МАНУАЛ по сборке
  (Facades/docs/FACADES_manual_2907.pdf + генератор): установка ×3,
  что исправлено (9 пунктов фидбэка), ATFZONE/ATFTABLE/ATCLAD/
  ATCLADDIM/ATFRAME по шагам (углы, Расчет/Вручную с таблицей
  исходных, оси Шаг/Точки без AClad, образцы кляммеров/направляющей,
  отчёт F2), тест-план 7 пунктов, план (штамп СК Профиль групп —
  ждём образец; маркировка; таблица максимумов по профилям).
  ЗАДАЧА СЛЕДУЮЩАЯ: фидбэк Германа по тест-плану → штамп-PDF
  статрасчёта → маркировка в знаках.**
- **27.07e — КРАСНЫЙ CHECK aa78fe8 (почтой Денису) РАЗОБРАН И ЗАКРЫТ:
  FacadesClassic использует dynamic, а Microsoft.CSharp был только в
  csproj ATableSpec (его ClassicUi) — реф добавлен во все три наших
  csproj (81406cf). mcs --parse этого НЕ ловит (ГРАБЛЯ-1 снова:
  parse ≠ компилятор; рефы csproj проверять при копировании
  паттернов с dynamic/WPF). «Собираем» №10: build-trigger `66a2728`
  = 81406cf (весь пакет 27.07a-e). Статус — глазами Дениса; PDF
  «ответ на фидбэк» (AFRAME_feedback_2707.pdf) переслать Герману
  ПОСЛЕ зелёной сборки.**
- **27.07d — ФИДБЭК ГЕРМАНА №2 (сборка №8/№9) — ШЕСТЬ ПУНКТОВ, ВСЕ
  РАЗОБРАНЫ И ЗАКРЫТЫ КОДОМ (кроме отложенных).** (1) Кнопки ленты
  «есть, но не работают»: панель добавлялась в живую ленту ДО
  наполнения кнопками (боевой ATableSpec наполняет → добавляет) —
  порядок Build исправлен во всех трёх FacadesRibbon; в КЛАССИКЕ
  меню «Фасады» не было вовсе → новый FacadesClassic.cs ×3 (общее
  выпадающее меню, каждый модуль идемпотентно свои пункты; паттерн
  боевого ClassicUi: COM dynamic, макрос «_CMD » без ^C^C; тулбары
  классики — позже по просьбе). (2) ГЛАВНОЕ — ATFRAME без AClad:
  при metas==0 и наличии контуров — диалог «Оси стоек [Шаг/Точки/
  Отмена]»: Шаг = первая ось точкой + шаг 608 по bbox (отступ 150),
  Точки = кликами; без rows_y — вопрос «шаг горизонтальных швов для
  кляммеров (0 — без)» → генерация от низа bbox. (3а) БАГ «нет
  расчётного пресета» на вертикальной: Расчет ставил систему
  Standart (без calc в systems.json) → теперь vertical+Расчет =
  «Вектор-1» с сообщением. (3б) Углы: перед шагами — клики «Точка
  на ВНЕШНЕМ углу здания» (0..N, Enter — по краям контуров);
  движок: corners_x → _corner_ivals/_in_corner/_hpos (полосы
  corner_zone вокруг углов; []=углов нет; None=края — совместимость);
  «Вручную» теперь СРАЗУ спрашивает шаг рядовой + шаг угловой,
  прочее (старт/зазор/зона) — под [Изменить]. (3в) Межэтажная:
  вопрос b_corner УБРАН — движок берёт шаг направляющих в угловой
  зоне из осей раскладки в угловых полосах (медиана интервалов),
  иначе 450 + note «проверьте по раскладке». (3г) Вставки ВС/В70 не
  раскладывались — это баг обёртки CLI, починен 27.07b (сборка №9);
  Герману — обновиться. (4) Образцы: помимо кронштейнов — образцы
  4 видов КЛЯММЕРОВ и НАПРАВЛЯЮЩЕЙ (динблок Германа: вставка база
  низ, растяжка = Double-свойство с наибольшим текущим значением —
  эвристика, фолбэк прямоугольник); PickBlock/InsertRailBlock.
  Тесты: FR-G1..G4 (углы слева/[]/авто-b_corner 450/из осей) +
  CLI corners — 63+71+8 зелёные; mcs --parse ×3 OK, дубли чисто.
  ОТВЕТЫ Германа по п.7: пролёт = расстояние между кронштейнами ✓
  (наше floor-правило согласовано), боковой кляммер = КЛУ-1.1 ✓
  (В-ж ЗАКРЫТ), максимумы шагов по профилям РАЗНЫЕ — таблицу
  запросить (В-о уточнён). П.6: все предложения пригодятся,
  ГЛАВНОЕ — PDF статрасчёта в штампе «СК Профиль групп» (след.
  заход; запросить реквизиты/образец штампа). НОВОЕ ТЗ (письмо
  ч.3): при выгрузке подсистемы УЧИТЫВАТЬ МАРКИРОВКУ из этапа 1
  (знаки наследуют МАРКИРОВКА/ЗАХВАТКА зоны — вместе с ведомостью
  через ATableSpec). ОТЛОЖЕНО: тулбары классики; библиотека блоков
  автоподгрузкой из DWG-файла (пока образцы кликами; DWG Германа в
  ПУБЛИЧНЫЙ бандл не кладём — конфиденциальность, решить с
  Денисом); hrails образцом (горизонтальные пока прямоугольники).
  Сборки НЕ БЫЛО — по команде.**
- **27.07c — «Собираем» №9 (команда Дениса) + PDF Герману
  AFRAME_update_2707.pdf (генератор make_update_2707.py): установка
  трёх бандлов, диалог [Расчет/Вручную] с шестью вопросами и откуда
  брать значения (таблица весов облицовок, анкера по основаниям),
  ЛОГИКА (методика = их эксель 1:1, подбор шага перебором до
  конструктивного max, честный отказ), тест-план (сверка с готовым
  статрасчётом объекта; межэтажная — проверить горизонтальные
  элементы после фикса CLI; отказ при анкере 300 Н), ПРЕДЛОЖЕНИЯ
  (автоподбор профиля с таблицей «профиль→шаг», лестница шагов по
  высоте, PDF-статрасчёт из программы, % запаса на чертеже, обратный
  подбор анкера), вопросы В-н/В-о + старые. ЗАДАЧА СЕССИИ-СЛЕД:
  фидбэк Германа по тестам и предложениям → правки → следующий блок.**
- **27.07b — ЭТАП 4 ВСТРОЕН В ЦЕПОЧКУ (целевой порядок Дениса:
  расчёт → шаги → расстановка).** Движок: `frame_plan` принимает блок
  `"calc"` (исходные конструктора: район/местность/высота/вес
  облицовки/вынос/анкер; пресеты кронштейн/удлинитель/профиль/вес
  направляющих — `systems.json["calc"]` у Вектор-1/Межэтажной/
  Ортогональной, переопределяемы) → `_apply_calc` вызывает
  `frame_calc.report` → шаги кронштейнов (вертикальная: верт. шаг;
  межэтажная/орто: гориз. шаг) ПОДМЕНЯЮТ справочник + note «ПО
  РАСЧЁТУ» + `calc_report` (цепочка проверок value/limit) в выходе,
  `summary.calc_steps`. Ни один шаг не прошёл → честный отказ с
  именами упавших проверок (профиль в углу → подсказка про b_corner —
  межэтажная с b=800 в углу не проходит по профилю, нужен шаг
  направляющих ~450 как у Нижнекаменской). b — медиана интервалов
  joints_x; rail_len — floor_step/медиана floors. FR-C1..C6 (13
  проверок): республиканская 800/450 сквозь frame_plan, отказы,
  регресс без calc, межэтажная 61.2 м → гориз. шаг 350 (ограничитель —
  удлинитель УК-85, ровно их верхний диапазон 45–65 м), орто 600/600.
  **Починен CLI frame_engine: sub_type/hrails/fittings ТЕРЯЛИСЬ в
  обёртке** (26.07d правил только frame_plan; межэтажная через exe
  выходила без горизонтальных профилей!) + calc/calc_report/
  полный summary пробрасываются; +2 юнита CLI (7 всего). C#
  FrameCommand: диалог «Шаги кронштейнов [Расчет/Вручную] <Расчет>»
  → район [Ia..V]/местность [A/B/C]/высота/вес (дефолт 8 межэтажной,
  25 иначе)/вынос/анкер Н (+b_corner для межэтажной, дефолт 450) →
  payload["calc"]; «Вручную» — прежний Принять/Изменить.
  PrintCalcReport: шаги + обе зоны с W_p и каждой проверкой
  value/limit («условия выполнены» — как их PDF). mcs --parse OK,
  дубли чисто, хелперы сверены; ПОЛНЫЙ компилятор = CI check на пуш.
  Тесты: 71 calc + 55 plan + 7 engine. Сборки НЕ БЫЛО (по команде
  после прогонов). Дальше: «соберём в кучу» → Герману итоговый
  расчётный модуль в составе сборки (выбор системы/профиля
  конструктором из справочника — расширение диалога), лестница
  высотных диапазонов, отчёт-PDF расчёта по образцу «Вектор фасад».**
- **27.07a — ЭТАП 4 НАЧАТ: движок frame_calc.py по 13 боевым
  статрасчётам «Вектор фасад» 2022–2026 (загрузил Денис; PDF + конспект
  METHOD_CALC.md в `atspec-testdata/docs/facades_calc/`, коммит
  f067f77). Методика ВОССТАНОВЛЕНА ПОЛНОСТЬЮ, вся арифметика всех
  блоков сверена руками:** пиковая ветровая СП 20.13330.2016
  (k/ζ степенные, тип A и B подтверждены на их числах, γ_в=1.4,
  c_p −1.2/−2.2), гололёд+два сочетания, грузовые площади по схемам
  (vertical: b×L1; interfloor: L2×L_напр; ortho: b×v_step),
  «k_нер» = коэффициент опорной реакции неразрезной балки — пары
  констант по числу пролётов (2: 0.125/1.25/0.0052; 3:
  0.100/1.10/0.00675; ≥4: 0.106/1.132/0.0063; правило n=max(2,
  floor(L/шаг)), подтверждено даже на нестандартном 450→6→1.132
  республиканской), анкер трёхчлен N_п·e1/e2+N_в·e3/e4+N_в (плечи =
  константы кронштейна, e1=вынос−Δ), кронштейн 1-1/2-2, удлинитель,
  профиль σ+прогиб (от НОРМАТИВНОЙ ветровой, f≤L/150), заклёпки
  срез/смятие. Справочник сечений: КР2-70/КР1-70/КР1-85/КП-125,
  УК-70/УК-85, ГП-40-40/ЗП-40-20/НСП-95-70/НСП-69-60/ШП-90-20/
  ШП-60-20(-20)/НШП-2. **71 юнит зелёный** — эталоны: монолит/кирпич
  (анкер 104/140≤306.1/244.9, кронштейн 1157/2024, профиль 452.9/817.7),
  Нижнекаменская тип-4 (281.2/277.6, 2080.9/2145.2, прогиб 7.9/8.2),
  Искра тип-5 (231.8/216.2, e3=0, 4 заклёпки), республиканская
  (кронштейн 1997/2039 — почти упор; **pick_step воспроизводит ИХ
  ВЫВОДЫ 800/450: при 500 кронштейн 1-1 валится**), Новгород-260
  ортогональная (72.4≤130.6; в углу их эксель непоследовательно взял
  3-пролётные — движок консервативнее по floor-правилу, эталон угла
  пересчитан руками), Регенбоген тип A. Соответствие схем: Тип-1 =
  наша vertical, Тип-4/5 = interfloor (5 без удлинителя, КП-125,
  e6=27), Новгород = ortho. Бугры-2018 = ПРЕДЫДУЩЕЕ поколение той же
  таблицы (γ_обл 1.2→1.1, R_y 2136.4→2250, z_e 15→факт. высота, без
  гололёда/k_нер, f≤L/200→L/150) — НЕ эталон. Реализационные решения:
  w_eff = max(ветер; гололёд+0.6·ветра) внутри цепочки; конструктивный
  max шага = вход max_step (дефолт 800). C#-интеграции НЕТ (следующий
  заход: шаги frame_calc → frame_plan вместо справочника, сокращение
  диалога ATFRAME; лестница высотных диапазонов как у Нижнекаменской
  350/400/500 по 24/45/65 м). Вопросы В-н..В-п — §Открытое.**
- **26.07e — «Собираем» №8 (Денис): build-trigger `a326397` = 020a2f1
  (весь пакет: 3 типа подсистемы + образцы + ATCLADDIM + лента).
  PDF Герману AFRAME_update_2607_2.pdf (что сделано по его письму,
  допущения v1 честно, установка всех трёх бандлов, тест-план по
  типам, вопросы) + генератор. СЕССИЯ ЗАВЕРШЕНА ХЭНДОФФОМ: промт
  переноса ОБНОВЛЁН (Facades/docs/HANDOFF_PROMPT.md, версия 26.07 —
  задача новой сессии: фидбэк по трём типам + ЭТАП 4 расчётный
  модуль, материалы загрузит Денис; копия с токеном отдана Денису
  файлом). Следующая сессия стартует по нему.**
- **26.07d — ПАКЕТ ПО КОМАНДЕ ДЕНИСА (без сборки — «соберём после
  прогонов»): МЕЖЭТАЖНАЯ и ОРТОГОНАЛЬНАЯ v1 + образцы блоков +
  ATCLADDIM + ЛЕНТА «Фасады» ×3.** Движок: sub_type
  vertical/interfloor/ortho; межэтажная (FR4/FR11): кронштейны по
  центру перекрытия с шагом ПО ГОРИЗОНТАЛИ (_hpos: углы 1500 чаще,
  крайние 150 от краёв, точки в проёмах пропускаются), НГП
  горизонтальный (hrails, рвётся окнами, note>6000), НСП в рустах
  БЕЗ смещения у окон (куски над/под окном = ШП-60-20), вставки на
  стыках НСП×НГП, СП-60-40 в подоконной зоне со скобами С1 (fittings);
  ортогональная (FR12/FR13): сетка кронштейнов (верт. 600 от
  низа+300, гориз. расчётный), ГП-40-40 на каждом ряду, ШП-60-20 по
  рустам, Z-профиль у окон (смещение 100, выступ 50), свес ≤300
  note. В-е (Денис): деление несущий/опорный в вертикальной ЕСТЬ —
  эвристика «первый от стыка = несущий» остаётся до слова Германа.
  Пресеты: Межэтажная наполнена, Ортогональная добавлена. Тесты
  42+5. C#: диалог «Тип подсистемы [Вертикальная/Межэтажная/
  Ортогональная]», отрисовка hrails прямоугольниками и метизов
  блоками (AFRAME_ВСТАВКА/AFRAME_СКОБА), кляммеры 4 видов (боковой
  с ножкой вбок, комбинированный с двумя), «Знаки [Условные/
  Образцы]» — образцы НЕСУЩЕГО/ОПОРНОГО кронштейнов с чертежа
  (DynamicBlockTableRecord, библиотека Германа «Кронш …»). AClad:
  команда ATCLADDIM (Ряд/Столбец → размерная цепочка по камням через
  GeometricExtents на слой _РАЗМЕРЫ_ОБЛ, паттерн AddDim ZoneCommand;
  в PackageContents добавлена). ЛЕНТА: FacadesRibbon ×3 модуля —
  ОБЩАЯ вкладка «Фасады», каждый плагин пересоздаёт только СВОЮ
  панель (Зоны+Ведомость / Раскладка+Размеры / Подсистема), иконки
  PNG в бандлах (Contents/icons, светлые под тёмную тему — урок
  ATableSpec 25.06), build.yml копирует icons. mcs все файлы OK,
  дубли чисто, хелперы OK. Рантайм НЕ проверялся, сборки НЕ БЫЛО.
  Допущения v1 и В-к/В-л/В-м — FRAME_TZ.md.**
- **26.07c — БОЛЬШОЕ ТЗ ГЕРМАНА (письмо, дословно в docs/FRAME_TZ.md)
  + ЕГО БИБЛИОТЕКА БЛОКОВ (testdata blocks/Bloki_dlya_raskladki_
  German_2607.dwg: «Кронш КР1/КР2/КПК/КО-<полка>-<вылет>», кляммеры
  рядовой/стартовый/половинка/угловой/комбинированный, динпрофили
  «Тип 1 и 2 / Тип 4 Вектор» 3000 база низ-центр, ВС-300/В-70/скоба
  С1; примеров «Огни залива» в файле НЕТ — запрошены В-и).
  РЕАЛИЗОВАНА ВЕРТИКАЛЬНАЯ СИСТЕМА ПО ТЗ: кронштейны НА КАЖДУЮ
  направляющую (300 от низа, 300 от верха, между равномерно ≤ шага;
  3000 → 300-800-800-800-300 ✓ FR1c), угловая зона 1500 (все
  системы), оконная стойка [низ−50, верх+50] (длина = сторона окна +
  100 ✓ FR7), доп. ось по центру плиты шире 600 (mid_rail_over=650 ✓
  FR10), кляммеры по видам: БОКОВОЙ на оконных стойках (В-а закрыт
  ТЗ: у отлива боковые), КОМБИНИРОВАННЫЙ на первом шве после стыка-
  термошва (✓ FR3), стартовый низ зоны/над откосом; «несущий» = 1-й
  кронштейн направляющей от стыка (В-е — уточнить); C#: вопрос
  «Кляммеры [Да/Нет]» (только керамогранит). Тесты 29+5 зелёные,
  mcs OK. МЕЖЭТАЖНАЯ и ОРТОГОНАЛЬНАЯ — концепт в FRAME_TZ, генераторы
  СЛЕДУЮЩИМИ заходами (НГП/НСП/ШП/СП/скобы С1 — блоки уже есть).
  В очереди также: образцы блоков Германа вместо наших знаков
  (+вылет/маркировка кронштейнов — ТЗ п.3), ATCLADDIM «размеры
  облицовки» (AClad), лента с иконками для всех модулей (прецедент
  RibbonUi ATableSpec). Вопросы: В-е, В-ж (боковой=КЛУ?), В-з
  (пример маркировки не дошёл), В-и (Огни залива).**
- **26.07b — «Собираем» №7 (Денис): build-trigger `9e2ecf5` =
  c18bfbb (фидбэк №1: окна/автоперекрытия/edge_offset). PDF Герману
  «обновление 26.07» (что было не так и почему, установка только
  AFrame, вопросы В-а…В-д, план развития 6 пунктов на согласование) —
  AFRAME_update_2607.pdf + генератор; отправлен Денису файлом.**
- **26.07a — ФИДБЭК ГЕРМАНА №1 ПО ATFRAME (скрины + разбор Дениса;
  сборка встала, знаки/слои живые) — ТРИ ЗАМЕЧАНИЯ ЗАКРЫТЫ.**
  (1) Стойки/кронштейны СКВОЗЬ ОКНА: C# пропускал полилинии БЕЗ метки
  ATCLAD (метка только на внешнем контуре!) → движок не знал о
  проёмах. Фикс: замкнутые полилинии выборки — геометрия ВСЕГДА
  (группировка по вложенности в движке), метки нужны только для осей;
  «ни одной метки» — ранний отказ с подсказкой. (2) Направляющие
  «бесконечной длины»: Герман не кликал отметки. Фикс: Enter без
  точек → вопрос «высота этажа (0 — без)» дефолт 3000 → floor_step в
  движок → автоперекрытия от низа контура (note) + note «направляющая
  длиннее хлыста 6000». (3) НОРМАТИВ «крепёж не ближе 100 мм от края
  проёма — бетон колется»: новый параметр системы edge_offset=100;
  ось ближе 100 к грани окна → на высоте окна кусок стойки СМЕЩЁН на
  ось «грань∓100», кронштейны и кляммеры куска — на смещённой оси;
  под/над окном — по оси руста (куски (lo,hi,x_eff) в движке).
  Тесты: 22→28 (FR7 смещение 996→900 с кронштейнами 2300/2900, FR8
  автоперекрытия 3000/6000, FR9 note хлыста) + 5 CLI
  (floor_step-проброс). mcs OK, дубли чисто. ОТКРЫТО (В22, к
  следующему прогону): стартовый кляммер на низе СМЕЩЁННОГО куска
  (сейчас ставится — как «начало куска»); точная граница смещённого
  куска = bbox окна (руст по низу/верху проёма); привязка стыка
  направляющих к отметке (центр, зазор 10). Ждёт «Собираем».
- **24.07-2 (Э2, эта сессия): КОМАНДА ATFRAME НАПИСАНА + БАНДЛ 5-м в
  CI.** FrameCommand.cs (самодостаточный, паттерн CladCommand, Run в
  try/catch→RunCore сразу): выбор зон с меткой ATCLAD (штриховки/
  марки/контуры; joints_x/rows_y ИЗ МЕТКИ — метка старой сборки без
  осей → предупреждение «перегенерируйте ATCLAD»), геометрия из
  fzones.json/полилиний, система кейвордами (классический
  конструктор, дефолт по не-OK — грабля 18.07r: Keywords.Default
  РОНЯЕТ компиляцию, прецедент TableCommand), подтверждение шагов
  [Принять/Изменить → 5 doubles], отметки перекрытий циклом точек,
  движок frame_engine.exe, вставка: направляющие = Polyline-
  прямоугольники (ширина rail_width системы), кронштейны/кляммеры =
  блоки-знаки на слоях Германа _01_ПС_*, метка ATFRAME + перегенерация
  (Erase любых Entity по handles). Знаки создаются кодом
  (EnsureSignBlock/EnsureClampBlock, сущности на слое «0»):
  НЕСУЩИЙ = ромб (квадрат 150 на 45°) с прямым крестом в круге r75
  («реже = несущий» — логика Дениса, частоты эталона 13/6/2),
  РЯДОВОЙ = квадрат с X-крестом в круге; кляммеры — прямоугольник
  100×12 (старт — с ножкой 40 вниз). ⚠ НОВЫЕ API без прецедента
  (BlockTableRecord/Polyline.AddVertexAt/Circle ctor) — компиляцию
  проверит check, рантайм знаков = первый прогон Германа. ATTDEF в
  знаках НЕТ (сознательно, меньше новых API) — добавить при
  подключении ведомости ATableSpec. МОСТ ИЗ ACLAD ГОТОВ:
  cladding_plan/clad_engine возвращают joints_x[] (стыки столбцов +
  грани проёмов ± полруста + юзер-русты) и rows_y[] (центры
  горизонтальных швов), CladCommand пишет их в метку ATCLAD (C34/
  C34b, 51+12 зелёные). frame_engine.py — CLI (op=frame, зоны
  facade_zone/1 + голые контуры, per_zone; 4 юнита). systems.json:
  + rail_width (60/40/60); поиск рядом с exe (PyInstaller onefile:
  __file__ в temp — искать sys.executable/_MEIPASS). build.yml: 5-й
  бандл AFrame.bundle.zip (dll+exe+systems.json); check.yml:
  компиляция AFramePlugin + юниты. mcs --parse OK, скан дублей чист,
  аудит хелперов OK. НЕ выкладывалось — ждёт «Собираем» и рантайма.
- **24.07 (Э1, эта сессия):** модуль создан; движок+справочник+22
  юнита зелёные; эталон подсистемы снят с боевого DWG Ленпроспекта
  aspose-cad'ом НАПРЯМУЮ (конверсия в DXF отвергнута — R12/2ГБ/блоки
  взорваны; скрипт-паттерн в журнале сессии, tools/dwg2dxf.py для
  простых случаев); конвенция Германа (слои/знаки/шаги) — в
  STAGE3_FRAME.md §Конвенция. check.yml дополнен тестами AFrame.
