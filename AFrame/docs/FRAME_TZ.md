# ТЗ Германа по подсистеме — письмо 26.07 (дословно) + статусы

## Дополнение: ответы 07.08 (кляммеры, стыки, зоны) ✓ реализовано

1. **Угловые (боковые) кляммеры:** вдоль оконных боковых откосов и
   боковых границ замкнутой области (внешний/внутренний угол);
   плитка шире 600 — боковой по середине плитки; установка бокового
   ВЕРТИКАЛЬНО; также под отливом и последним рядом по высоте
   (горизонтально). ✓ 07.08b (`_piece_clamps`/`_edge_top_clamps`:
   рядовой на оси-русте, боковой на оси в поле плиты; orient v/h).
2. **Кронштейны/стыки — обе схемы верные:** точки перекрытий указаны
   → несущий на центре перекрытия + опорные (В15); НЕ указаны →
   снизу вверх с заданным шагом (хлысты 3000+зазор от низа зоны,
   «300 от торцов + ≤ шага», все кронштейны одного типа — «когда
   везде монолит»). ✓ 07.08b (`lash`-режим).
3. **Ведомость:** наше и ручное считать вместе, не разделяя; нужна
   проверка задвоений — полностью совпадающие координаты → оставить
   один. ✓ 07.08b (команда ATDEDUP; на полигоне 84 дубля).
4. На полигоне ATFRAME **запускался** (атрибуция смешанная).
5. **Угловая зона — только внутрь** от указанной точки. ✓ 07.08b.
6. **Выбор слоя из списка существующих** везде, где спрашивается
   слой. ✓ 07.08b (ATCLAD `LayerPickForm`, ATFZONE комбо со слоями;
   ATCLADDIM/ATFRAMEDIM были с 04.08g).

## Дополнение: письмо 01.08 (профили + размеры) + статусы

1. Ручная раскладка-пример: `полигон.dwg` →
   `atspec-testdata/dxf/facades/poligon_0208/` (три участка —
   Рядовая / Межэтажная / ортогональная; ручные кляммеры, кронштейны
   «Кронш КР1-85-150 / КР1-70-100», профили-динблоки, метизы РЯДОМ с
   нашей генерацией `_01_ПС_*` — диф-эталон).
2. «В динамическом блоке в названии видимости прописаны чёткие
   названия направляющих. Хотелось бы чтоб программа меняла видимость
   и рисовала профиль, фактически выбранный пользователем — это
   позволит менять профиль сразу на всём чертеже».
   Допустимые профили: **вертикальная** — ГП-40-40, ГП-60-40,
   ШП-60-20 (вертикально); **межэтажная** — НГП (горизонтально),
   НСП-1/НСП-2 (вертикально), СП-60-40 (под окно, горизонтально);
   **ортогональная** — ГП-40-40/ГП-60-40 (горизонтально),
   ШП-60-20/ZП-40-20 (вертикально).
   ✓ РЕАЛИЗОВАНО 02.08: видимость по маркам (нормализованный матч,
   Z↔З), выбор профиля в диалоге (вертикальная Авто/ГП40/ГП60/ШП60,
   межэтажная НСП1/НСП2), В-ш закрыт списками.
3. Команда размеров между кронштейнами «по аналогии с размерами
   облицовки»: запрос столбец/строка → размеры между ЦЕНТРАМИ.
   ✓ ATFRAMEDIM 02.08 (слой `_РАЗМЕРЫ_ПС`).

> Письмо перекрывает/уточняет прежние правила там, где противоречит.
> Блоки — `atspec-testdata/dxf/facades/blocks/
> Bloki_dlya_raskladki_German_2607.dwg` (кронштейны «Кронш
> КР1/КР2/КПК/КО-<полка>-<вылет>», кляммеры рядовой/стартовый/
> половинка/угловой/комбинированный, динпрофили «Тип 1 и 2 / Тип 4
> Вектор» — 3000 мм, база низ-центр ±70, растяжка+видимость;
> В-70, ВС-300, скоба С1-50, полоса-перемычка, швеллер угловой).
> Примеров раскладки «Огни залива» в файле НЕТ (только витрина
> блоков) — запрошены отдельно.

## Три типа подсистемы (дословно, сжато)

**1. Вертикальная** ✓ РЕАЛИЗОВАНО 26.07c (наш текущий генератор):
- направляющая в руст, центр руста = центр направляющей ✓;
- кронштейны ПО НАПРАВЛЯЮЩЕЙ: первый 300 от НИЗА, последний 300 от
  ВЕРХА направляющей, между ними равномерно с шагом ≤ расчётного
  (3 м → 300-800-800-800-300 = 4 шт); нестандартная длина — крайние
  300/300, между ними ≤ шага ✓;
- угловая зона = 1500 от края облицовки, шаг меньше ✓ (corner_zone
  1500 во всех системах);
- зазор направляющих по высоте 10 ✓;
- у окна: направляющие слева/справа на 100 от края замкнутой
  области, длина = сторона окна + 100 (выступ 50 сверху и 50 снизу)
  ✓ (кусок [низ−50, верх+50] на оси грань∓100);
- облицовка шире 600 → доп. направляющая по центру плиты ✓
  (mid_rail_over=650: пролёт осей больше порога → ось в середине).

**2. Межэтажная** ✓ РЕАЛИЗОВАНА v1 26.07d (FR4/FR11; допущения ниже):
- кронштейны по центру КАЖДОГО ПЕРЕКРЫТИЯ с расчётным шагом ПО
  ГОРИЗОНТАЛИ (угловая зона 1500 — шаг меньше);
- на кронштейны удлинители → ГОРИЗОНТАЛЬНЫЙ несущий профиль НГП;
- вставки ВС-300/В-70; в русты — вертикальные НСП1/НСП2 (центр руста);
- у окон направляющие по центру руста (БЕЗ смещения 100);
- простенок между окнами: вертикальные ШП-60-20 на НГП, высота от
  верха до низа замкнутой области; в подоконной зоне — горизонтальный
  СП-60-40, крепится к крайним межэтажным профилям скобами С1;
- диалог: запрашивать шаг перекрытия ✓ (floor_step уже есть).

**3. Ортогональная** ✓ РЕАЛИЗОВАНА v1 26.07d (FR12/FR13; допущения ниже):
- «сетка» кронштейнов: шаг по вертикали 600, по горизонтали расчётный;
- удлинители → горизонтальный ГП-40-40 → вертикальные ШП-60-20 по
  центрам рустов; зазор вертикальных 10; консольный свес ≤ 300;
- у окон Z-образные направляющие, длина = сторона окна + 100,
  выступ 50/50.

Комбинации типов в одном проекте — допустимы (зоны считаются
независимо, тип спрашивается на каждый запуск ✓).

## Диалог программы (по Герману) — статус

1. Замкнутые области ✓. 2. Тип подсистемы ✓ (пока все три ведут
   себя как вертикальная; межэтажная/ортогональная — заглушки до
   своих генераторов). 3. Тип кронштейнов И ДЛИНА (в проекте один
   тип разной длины → маркировать по-разному: цвет/визуальные
   различия) — В ОЧЕРЕДИ (образцы блоков «Кронш …» из его файла +
   маркировка). 4. Межэтажная → шаг перекрытия ✓ (floor_step).
   5. «Кляммеры раскладывать?» [Да/Нет] ✓ 26.07c (только
   керамогранит — решает конструктор).

## Кляммеры (правила Германа) — статус

- рядовой на стыке в рядовой зоне ✓;
- БОКОВОЙ в зоне бокового примыкания к окну ✓ 26.07c (все кляммеры
  смещённых оконных стоек — «боковой»);
- стартовый: первая плитка (низ зоны) ✓ + над верхним откосом ✓;
- у отлива — боковые ✓ (низ смещённой оконной стойки — боковой,
  НЕ стартовый; закрыло и наш вопрос В-а);
- термошов направляющих: в ПЕРВЫЙ руст после шва — комбинированный
  (или боковой+стартовый) ✓ 26.07c («комбинированный» на первом шве
  выше каждой отметки стыка).

## Допущения v1 (проверить на прогоне Германа)

- Межэтажная: ШП-60-20 = куски осей НАД/ПОД окнами (простенки между
  окнами по горизонтали пока несут сквозные НСП — В-к); СП-60-40 на
  отметке НИЗА окна между крайними осями слева/справа окна, скобы С1
  на его концах; вставка (ВС-300/В-70 одним знаком) на каждом стыке
  НСП×перекрытие; кронштейны, попавшие в проём, пропускаются.
- Ортогональная: сетка от низа зоны + 300, шаг 600 (В-л); Z-профиль
  у окна = смещение 100 + выступ 50 (как вертикальная — В-м);
  контроль консольного свеса ≤300 note'ом.
- Диалог: тип подсистемы вместо выбора производителя («Вектор-1»
  остаётся пресетом в systems.json, в диалог вернём со справочником).

## Открытые вопросы Герману (В-е…)

- **В-е**: в ВЕРТИКАЛЬНОЙ системе есть ли деление кронштейнов
  несущий/опорный (в ТЗ его нет — только шаг)? Сейчас: первый
  кронштейн направляющей, начинающейся стыком на перекрытии, помечаем
  несущим, остальные рядовые. Так?
- **В-ж**: «боковой кляммер» = «угловой!КЛУ-1.1-н» из твоего файла?
- **В-з**: маркировка кронштейнов на фасаде (пример обещан) — файл с
  примером не дошёл, пришли; пока маркируем именем блока.
- **В-и**: примеры раскладки «Огни залива» (межэтажная + вертикальная)
  — в файле блоков их нет, пришли отдельно (это эталоны для
  межэтажного генератора).

## Блок 2 (AClad) — просьбы Германа, В ОЧЕРЕДИ

- Команда «размеры облицовки»: выбрать горизонтальный ряд →
  проставить ширины всех плит ряда; вертикальный столбец → высоты
  (паттерн размеров — ATFZONE/«Проба 4», прецедент в ZoneCommand).
- Панель с иконками (лента) для частей 1 и 2 (и 3) — прецедент
  RibbonUi.cs ATableSpec (боевой), после приёмки функционала.
