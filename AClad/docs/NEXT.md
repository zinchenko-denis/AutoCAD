# AClad — состояние и хэндофф (ЧИТАТЬ ПЕРВЫМ)

> **Это проект AClad — раскладка облицовки НВФ (этап 2 фасадного
> направления), ОТДЕЛЬНЫЙ бандл. НЕ путать с LD/физикой.** Выделен из
> Facades 22.07 по просьбе Германа: боевой этап 1 (AFacades: зоны/
> ведомости) ставится один раз, раскладка переустанавливается при
> каждой итерации. Грабли среды/сборки/push — `ATableSpec/docs/NEXT.md`
> (обязательны), методика фидбэка — `ABlockGen/docs/NEXT.md`, история
> направления и этапа 1 — `Facades/docs/NEXT.md`. Ветка общая:
> `feat/auto-reactor`; `main` — только по «Пушим».

## TL;DR
- **Что:** команда **ATCLAD** — раскладка облицовки универсальным
  блоком (кассетой) по зонам ATFZONE и/или замкнутым полилиниям.
  Гибрид: тонкий C# (`ACladPlugin.dll`) + замороженный движок
  (`clad_engine.exe`); бандл `AClad.bundle` в релизе `latest`.
- **Действующее ТЗ — письмо Германа 22.07** (дословно + статус:
  `docs/CLADDING.md` §ТЗ 22.07). Оно ЗАМЕНЯЕТ прежние В1–В8 при
  противоречиях: п.1.2 (целая по центру простенка, в обе стороны,
  кусок <300 — сдвиг на полплиты) вместо В5; режимов «Проемы/Край»
  больше нет — алгоритм один.
- **Связка с AFacades:** контракт данных общий (Xrecord «ATFZONE» на
  штриховках/марках читаем, свой ключ «ATCLAD» пишем;
  `<dwg>_fzones.json` — геометрия зон), реализация продублирована
  СОЗНАТЕЛЬНО (модули развязаны, обновляются независимо).
- **Статус на 07.08 (шапка обновлена — прежняя 22.07 врала «сборка
  не выкладывалась»):** ATCLAD и ATCLADDIM — боевой пилот у Германа,
  бандл в `latest` со сборки №12; несколько кругов фидбэка отработано
  (журнал — §Статус ниже). Движок `cladding_plan` 58 юнитов + CLI 12
  + краш-аудит 124 сценария — зелёные; допуск ортогональности
  контура `ortho_snap` 5 мм (04.08e). НЕ реализовано из ТЗ 22.07:
  **1.4/2.6** (точки горизонтальных рустов → локальные вертикальные
  датумы над окнами) и **1.3-хвост** (целая шире окна с перекрытием
  соседних пролётов).

## Файлы
- `engine/cladding_plan.py` — правила раскладки КАССЕТ (ТЗ 22.07 в
  докстринге); `engine/clad_engine.py` — CLI (op=cladding | tile_pattern,
  группировка голых контуров по вложенности — своя, от facade_zones НЕ
  зависит); `engine/test_*.py` — юниты (`PYTHONUTF8=1`, запуск из engine).
- **`engine/tile_pattern.py` — МЕЛКОШТУЧНАЯ раскладка по образцу**
  (op=tile_pattern, команда ATTILE): плитка/кирпич с перевязкой в
  шахматку и раскраской по нарисованному раппорту. Правила/модель —
  `docs/TILE_PATTERN.md`; юниты — `engine/test_tile_pattern.py` (91).
  `tools/tile_pattern_dxf.py` — обкатка без AutoCAD (ezdxf → DXF+xlsx),
  фикстура `tools/testdata/tiles290/`.
- `src/ACladPlugin/CladCommand.cs` — ATCLAD (диалог ТЗ 2.1–2.5,
  вставки, метка «ATCLAD» с параметрами и хэндлами, перегенерация
  удалением прежних); `Plugin.cs` — BuildStamp; сборка — build.yml
  (бандл 4-й), проверка — check.yml.
- `docs/CLADDING.md` — ТЗ дословно + модель данных/алгоритм;
  `docs/ATCLAD_manual.md` — инструкция Герману.

## Дальше (порядок ценности)
1. Фидбэк теста Германа по сборке с Г1–Г3; его DWG-эталон (просьба
   остаётся) → D-сверка в тестах.
2. МАРКИРОВКА/ЗАХВАТКА камней (В10), ведомость через ATableSpec.
3. Кнопки ленты (после приёмки), пресеты параметров из метки ATCLAD.

## Вопросы Герману
Открыты: В10 — только МАРКИРОВКА камня (типоразмер?); ЗАХВАТКА
закрыта 01.08b (= зона этапа 1, ответ Дениса: кассеты заполняют
zone_id в attref-цикле; подробно — AFrame/docs/NEXT.md §В-ю).
В11 (арочные контуры). Г1–Г3 ОТВЕЧЕНЫ 22.07 и реализованы
(см. §Статус 22.07c).

## Статус
- **23.09n — ФИДБЭК ГЕРМАНА ПО №24 (текстом через Дениса) + ИСПРАВЛЕНИЕ COPY.**
  Раздел 6 PDF №24 («что проверить»): п.1 образец динблоком на 2–3 слоях, обе
  галки → плитки на слоях образца, тем же блоком, та же видимость, новых слоёв нет —
  РАБОТАЕТ; п.2 галка «слои и блоки образца» снята → свои слои, как раньше —
  РАБОТАЕТ (риск сдвига подрезки из 23.09m на его блоках не проявился — в плане,
  приоритет ниже); п.3 COPY зоны вместе с раскладкой → ATTILE на копии: оригинал
  цел, НО НА КОПИИ НОВАЯ РАСКЛАДКА ЛЕГЛА ПОВЕРХ СТАРОЙ — исправлено (68ffa92), ждёт
  живой проверки в №25. Пп.4–9 (скос, повторный ATFZONE, ATDEDUP, ATFRAME с
  расчётом, Отменить, меню после отмены выхода) — ответа нет, повторить в PDF №25.
  Раздел 7, п.4: «правило рустов ATTILE (за рустом — заново от руста)» — «Правило
  подходит» — ЗАКРЫТ (про «только кляммеры» отдельно не сказал — см. AFrame).
  ПРИЧИНА наложения: метка хранила хэндлы строками, COPY их не переводит — метка
  копии указывала на плитки оригинала (правка 24.09c их верно не трогает), а
  скопированные плитки не были указаны нигде → их не удалял никто.
  ИСПРАВЛЕНИЕ (ATTILE, ATCLAD; то же в ATFRAME): метка = строки + мягкие ссылки 330
  на свои объекты + признак `refs`; при COPY AutoCAD переводит ссылки на клоны,
  скопированные той же командой. Ссылка, чьего хэндла нет среди строк, — клон
  копии: удаляется, когда копия получает новый результат (OwnedLabels.Clones).
  Попутно найдено: копия зоны ATFZONE без повторного ATFZONE имеет тот же номер, а
  геометрия в _fzones.json — оригинала: раскладка легла бы на МЕСТО ОРИГИНАЛА
  (невидимо, точно поверх его плиток). Теперь сдвиг штриховки относительно
  _fzones.json (одинаковый вектор по обоим углам габарита) → зона пропускается:
  «выполните ATFZONE на копии». Ограничения: копии, сделанные до №25 (ссылок нет),
  — предупреждение, скопированные плитки удалить вручную; зона с дугами на краю
  габарита — сдвиг не ловится. Проверка исходников — `tools/label_refs_check.py`
  (в check и build). Поведение deep clone (перевод ссылок 330 при COPY) здесь не
  проверить — только вживую. В PDF №25: разложить сборкой №25 → COPY с раскладкой →
  ATTILE на копии (одна раскладка, оригинал цел); COPY зоны ATFZONE → ATTILE сразу
  (ждём сообщение) → ATFZONE на копии → ATTILE.
- **23.09m — разбор кода образца из блоков (23.09j); код НЕ правили — решение
  Дениса: ждём фидбэк Германа по №24.** Вставка плиток блоками образца до сих пор
  проверена только компиляцией. Найдено:
  (1) Подрезка динамическим блоком образца ставится со смещением «точка вставки →
  левый нижний угол» плитки ОБРАЗЦА (OffX/OffY), и только потом ей меняют
  «ширину»/«высоту». Верно, лишь если блок тянется от левого нижнего угла. База в
  центре + растяжка в обе стороны → кусок сдвинут на (W−w)/2 (плитка 290, кусок
  100 → 95 мм); растяжка влево → на W−w; у верха/низа зоны — так же по высоте.
  Целые плитки не страдают (размер = образцу). Полилиний/штриховок не касается.
  (2) Размер не выставился (TrySetNum = false) — только dynFail++ и число в конце;
  вставка ОСТАЁТСЯ в чертеже с размером по умолчанию из определения (ApplyLook
  «ширину»/«высоту» сознательно не переносит). Если AutoCAD молча подгоняет
  значение к списку/шагу параметра — не ловится вовсе (как именно он себя ведёт —
  не проверено).
  (3) НЕ ПРОВЕРЕНО: CellExtents не смотрит видимость кривых — у блока плитки с
  состояниями видимости габарит образца, вероятно, считается по всем состояниям.
  План (одна правка на 1 и 2): после установки размеров — габарит вставки по
  CellExtents → сдвиг его минимума точно в (x, y); расхождение с (w, h) > 0,5 мм →
  вставку удалить, кусок — контуром на тот же слой, число — в сводку. В №25 вместе
  с фидбэком Германа. В его приёмку: образец динблоками с базой НЕ в левом нижнем
  углу; блок плитки с состояниями видимости.
  Автоблок «по имени без проверки размера» перепроверен: формат зашит в имя
  (BondSettings.BlockFor) — риск низкий, как в docs/REVIEW_RESPONSE_24.09.md.
- **23.09 — «Собираем» №24 (Денис) — ВЫЛОЖЕНО.** build-trigger `e8333e1` = код
  `5509395` + патч build.yml; build-bundle #97: test → build, обе зелёные.
  Проверено СКАЧИВАНИЕМ: пять архивов latest = build-97 (AFacades 8 182 607 Б,
  AClad 8 255 215 Б, AFrame 8 228 484 Б); build-info.json — сборка 97, коммит
  e8333e1; манифесты фасадных бандлов R24.0–R24.3; в ACladPlugin.dll —
  «слои и блоки образца», «Плитки — на слоях образца», выбор образца блоками;
  тег build-97 → e8333e1. Состав: всё из №23 (исправления по рецензии) +
  образец ATTILE из блоков/слои образца (запрос Германа) + манифесты 2021–2024.
  ОБЪЕДИНЁННЫЙ PDF Герману — `AFrame/docs/FACADES_build24_2309.pdf`
  (генератор `make_build24_2309.py`, картинки `build24/`, `make_pics24.py`).
- **23.09j — ЗАПРОС ГЕРМАНА (видео прошлого захода; тогда не был записан ни
  в NEXT, ни в инструкции — промах; повторён письмом 23.09): «образец рисовать
  динамическими блоками; блоки образца разных цветов разнесены по слоям —
  раскладка по этим же слоям, без новых».** СДЕЛАНО (в ветке, CI): выбор
  образца — полилинии, штриховки и БЛОКИ (в т.ч. динамические; габарит — по
  кривым определения); галка окна «слои и блоки образца» (по умолчанию вкл.):
  плитки типа T (целые и подрезка) — на слой T образца, новых слоёв нет;
  элемент типа — блок образца (динамический — и для подрезки, с видимостью/
  цветом/динсвойствами образца; обычный — для целых своего формата, подрезка
  — полилиниями на том же слое); повёрнутый/масштабированный блок образца —
  сообщение, тип как раньше. Манифесты фасадных бандлов — R24.0–R24.3
  (AutoCAD 2021–2024; у Германа 2024). Живой прогон — у Германа.
- **23.09 — «Собираем» №23 (Денис) — ВЫЛОЖЕНО.** build-trigger `4a06f23` = код
  `f743f3f` (исправления по независимой рецензии) + патч build.yml;
  build-bundle #96: задача test (юниты, аудиты, контракт, стыки, синтетика) —
  зелёная, затем build — зелёная. Проверено СКАЧИВАНИЕМ: все пять архивов
  latest = build-96 (AFacades 8 184 382 Б, AClad 8 252 731 Б, AFrame 8 227 330 Б);
  build-info.json внутри (сборка 96, коммит 4a06f23, Python 3.12.10,
  PyInstaller 6.22.3); тег build-96 → 4a06f23 (впервые на собранный коммит);
  строки новых сообщений в DLL и новые функции в движках на месте. PDF
  Герману — `AFrame/docs/FACADES_build23_2309.pdf` (генератор
  `make_build23_2309.py`, картинки `build23/`, `make_pics23.py`). Обновлять
  ВСЕ ТРИ фасадных бандла. NB: в заголовках коммитов этой работы стоит «24.09»
  — фактически всё сделано 23.09.
- **24.09b/c/f/g — ПО НЕЗАВИСИМОЙ РЕЦЕНЗИИ 23.09 (в ветке, сборка не
  выпускалась; ответ — `docs/REVIEW_RESPONSE_24.09.md`).** Движок: полная
  вложенность контуров в `clad_engine` (U-полоса вокруг отдельной зоны
  больше не пропадает); `bridge_axes` ~50× быстрее (100×100 м: ~95 → 1.8 с,
  оси те же). C# ATTILE/ATCLAD: прежнее — по владельцам (`OwnedLabels`):
  удаляется только у зон с результатом, скопированная COPY метка (owner ≠
  хэндл носителя) ничего не удаляет, у старых меток без owner — только
  элементы на выбранных зонах; выбор части общей раскладки добирает
  остальных участников; метка — одна на владельца (объединение частей);
  изменённая после ATFZONE зона не раскладывается; полилинии зоны в выборке
  заменяются вместе с зоной. Метки — `schema: 2`, `owner`. attile_contract —
  48 ключей, без ложных «неизвестных». Юниты 173 + 12 + 58, фикстура 145 333,
  синтетика 741/0. ОТКРЫТО: живая приёмка (COPY/Undo/частичный выбор),
  dynFail динблоков, автоблок по имени.
- **23.09x — «Собираем» №22 (Денис) — ВЫЛОЖЕНО.** build-trigger `df52019` = код
  `afda8d5` + патч build.yml; build-bundle #95 (id 35873372903) зелёный, релиз
  `build-95`, `latest` = build-95 (проверено СКАЧИВАНИЕМ и сравнением архивов:
  AFacades.bundle.zip 8 177 554 Б, AClad.bundle.zip 8 245 273 Б, AFrame.bundle.zip
  8 219 827 Б; DLL от 23.09 14:22 — сроки ожидания движков 120/600/300 с внутри;
  движки распакованы из exe — E_CONTOUR_CROSSES, per_zone-оси, _vspans/_hspans,
  _pt_in_poly на месте). Состав — исправления ревью 23.09r–u (ATFRAME по
  контуру стены, зоны ATFZONE, оси по зонам, без задвоения, группировка по
  полигону, стойка у окна у края, СП межэтажной; ATFZONE — фантом-зоны, дуги,
  метры; сроки движков). Обновлять ВСЕ ТРИ фасадных бандла. PDF Герману —
  `AFrame/docs/FACADES_build22_2309.pdf` (генератор `make_build22_2309.py`,
  картинки `build22/`, `make_pics22.py`).
- **23.09v — по ревью (в ветке, в сборку №21 НЕ вошло):** clad_engine отдаёт
  оси швов ПО ЗОНЕ (per_zone.joints_x/rows_y), ATCLAD пишет в метку зоны
  свои оси, а не объединение всего прогона (ATFRAME ставил в зоне стойки по
  швам соседней); срок ожидания движка 600 с (ATCLAD и ATTILE). Юниты
  кассеты 58 + CLI 12, плитка 173 — зелёные.
- **23.09p — «Собираем» №21 (Денис) — ВЫЛОЖЕНО.** build-trigger `a5dde33` = код
  `51e5533` + патч build.yml; build-bundle #94 (id 35866941847) зелёный, релиз
  `build-94`, `latest` = build-94 (проверено СКАЧИВАНИЕМ и сравнением архивов:
  AClad.bundle.zip 8 242 999 Б, ACladPlugin.dll 96 768 Б от 23.09 13:27 — внутри
  раздел рустов, «Прежние/Новые/Нет», зоны из _fzones.json, лента/меню ATTILE;
  AFrame.bundle.zip 8 213 840 Б, AFramePlugin.dll 77 824 Б — окно ATFRAME и
  «только кляммеры»; движки распакованы из exe — `forced_cells` и
  `_clamps_on_rails` на месте). Состав: русты ATTILE, ATTILE по штриховке
  ATFZONE (23.09o), ATCLAD снят с ленты/меню (команда — набором), окно ATFRAME и
  режимы «что раскладывать». PDF Герману — `AClad/docs/FACADES_build21_2309.pdf`
  (генератор `make_build21_2309.py`, картинки `build21/`).
- **23.09j/k — ФИДБЭК ГЕРМАНА (видео 23.09): «оставить одну команду ATTILE,
  добавить принудительные русты из ATCLAD».** На видео окно ATTILE открыто в
  живом AutoCAD 2024 (первый живой запуск): КГ на НВФ 600×1200, шов 8/8,
  элемент — динблок «кассета 2», без смещения, отсчёт ЛН, общая точка, руст
  вокруг проёмов, Г-куски «резать, руст по продолжению грани»; окно на
  Windows без обрезаний. СДЕЛАНО (CI #153/#154 зелёные, сборка не
  запускалась):
  движок `tile_pattern` — `vjoints` (ось вертикального руста, ТЗ 2.5) и
  `hjoints` (НИЗ горизонтального, 2.6/Г3): руст — граница участка, не
  облицовывается; в опорной ячейке (базовая целая по «Отсчёту»/общей точке)
  фаза прежняя, за рустом раскладка заново ОТ РУСТА (целая плитка базового
  ряда у руста, подрезка к дальней стороне; для отсчёта снизу = Г3 ATCLAD);
  клик у грани проёма сливается со швом грани; у края зоны — нота, не
  ставится; действует на все зоны прогона; столбцы — в реальной плоскости.
  Без рустов движок прежний (фикстура 145 333). Юниты 152 → 173 (T38–T45),
  attile_synth + семейство forced (I13/I14) — 3 сида × 741, 0 провалов.
  окно — раздел «6. Принудительные русты (как в ATCLAD)» (две галки);
  команда после окна и общей точки спрашивает точки как ATCLAD; прежние
  точки (метка ATTILE, иначе ATCLAD) — «[Прежние/Новые/Нет] <Прежние>»;
  точки пишутся в метку ATTILE. mono UiCheck OK, контракт 39 ключей.
  ОТКРЫТО: убирать ли ATCLAD с ленты/меню (Герман: «одна команда») — решает
  Денис; ATTILE по штриховке ATFZONE не раскладывает (находка ревью 23.09,
  отложена Денисом — `docs/REVIEW_23.09.md`) — Герману выбирать полилинии.
- **23.09i — РЕВЬЮ (сессия полного разбора; поведение НЕ менялось; отчёт —
  `docs/REVIEW_23.09.md` в корне).** Прогон: cladding_plan 58, CLI 12,
  tile_pattern 152, краш-аудит 124 — зелёные; attile_synth сиды 2309/77/5 —
  3×696 сценариев, 920 954 куска, 0 провалов (воспроизведено); окно под mono —
  ОК (сборка: `mcs -r:System.Windows.Forms.dll -r:System.Drawing.dll
  -r:System.Web.Extensions.dll tools/attile_ui/UiCheck.cs src/ACladPlugin/BondForm.cs
  src/ACladPlugin/BondSettings.cs`, запуск `xvfb-run -a mono UiCheck.exe DIR`);
  attile_contract — 35 ключей, 0 недостающих; фикстура 290×82 — 145 333.
  НАЙДЕНО (доказательства — `tools/xmod_check.py` в корне):
  (1) КРИТИЧНО: ATTILE по штриховке/марке зоны ATFZONE отказывает — в запрос
  идёт метка ATFZONE без геометрии (`TilePatternCommand.cs:128–140`, с 09.09);
  ATCLAD берёт геометрию из `_fzones.json` и вычитает полилинии зоны из
  «голых» — взять оттуда (без вычета после починки зона разложится дважды).
  (2) ВАЖНО: метка ATCLAD несёт оси швов ВСЕГО прогона (`clad_engine.py:216`,
  `CladCommand.cs:570`) — у ATTILE правильно, по зоне.
  (3) ЗАМЕР: метка ATTILE на фикстуре — 183 746 хэндлов, крупнейшая 345 КБ
  (1 414 строк Xrecord) × 4 объекта, всего 2.8 МБ; предложение — хэндлы вне
  метки (NOD/XData).
  (4) НЕ ПРОВЕРЕНО ВЖИВУЮ: одна транзакция на ~150 тыс. объектов с
  EvaluateHatch у фигурных — время/память на тесте Германа.
- **23.09 — ATTILE СТАЛА УНИВЕРСАЛЬНОЙ РАЗБЕЖКОЙ (заявка «шахматный порядок
  для любой облицовки»), СОБРАНО: сборка №20 = build-bundle run #93, релиз
  `build-93` (build-trigger `db446b7` = `8c31b96`), `latest` обновлён (API
  листинга релизов отдаёт устаревшие updated_at — проверять СКАЧИВАНИЕМ:
  AClad.bundle.zip 8 236 106 Б, DLL 23.09 09:47, acl_tile.png; движок из
  exe распакован pyinstxtractor-ng — новые функции внутри). check #150/#151
  зелёные. PDF Герману — `docs/ATTILE_manual_2309.pdf` (генератор
  `make_attile_2309.py`, картинки `docs/attile_2309/`).
  Решение: разбежка — в ATTILE, НЕ в ATCLAD (правила ТЗ 22.07 — целая по
  центру простенка, сдвиг на полплиты при подрезке <300 — несовместимы с
  регулярной перевязкой). Окно `BondForm` (+ модель `BondSettings`, чистая
  логика, собирается под mono): материал-пресет (КГ НВФ / фиброцемент-HPL /
  камень / клинкер-кирпич / другое), формат+⇄, швы, ряды|столбцы, смещение
  none|alternate|step|sequence|pattern (доли МОДУЛЯ: 1/2 = стык по центру
  плитки соседнего ряда, или мм; направление), отсчёт 9 точек габарита или
  основной стены + «плитка/шов по оси» + общая точка, руст вокруг проёмов,
  Г-куски keep|split|split_joint, «не класть тоньше», «малая подрезка» (150
  = В4), пропил, элемент — прямоугольник или ДИНБЛОК «ширина/высота» (как
  ATCLAD: все камни блоком, ЗАХВАТКА). Живой просмотр — та же формула
  сдвига, что движок (сверено на 10 наборах). Поток команды: зоны → окно
  (из метки зоны или последние значения, %APPDATA%\AClad\attile_settings.json)
  → «Заменить?» раскладку ATCLAD → образец/точка → движок → чертёж.
  Движок `tile_pattern.py`: ключи bond/axis/anchor/gap_around/shaped/
  warn_cut (без них — поведение 09.09 один в один; фикстура 290×82 — новый
  = старый движок побайтно по счёту), мост ATFRAME per_zone.joints_x/rows_y
  по фактическим стыкам. ATFRAME читает метку ATTILE, если нет ATCLAD;
  ATCLAD снимает ATTILE на своих зонах. Кнопка «Разбежка» на ленте, пункт
  меню, строка загрузки.
  ПРОВЕРКИ: юниты 152 (91 старых + 61 новых), `tools/attile_synth.py` —
  независимый оракул shapely, 12 инвариантов, 3 сида × 696 сценариев ≈ 920 тыс.
  кусков — 0 нарушений; `tools/attile_ui/UiCheck.cs` (mono+xvfb: логика,
  пересечения контролов, ОК/отказ, снимки окна); `tools/attile_contract.py`
  (35 ключей ответа, которые читает C#, — в ответе на C#-запрос).
  ГРАБЛИ, НАЙДЕННЫЕ СИНТЕТИКОЙ (все исправлены, регресс-тесты T33–T37):
  (1) подрезанная плитка через ГОРИЗОНТАЛЬНЫЙ шов смежных зон делилась на
  два куска (в столбцах — через ЛЮБОЙ шов стен): касающиеся интервалы полосы
  не сливались; (2) перекрывающиеся зоны сливались как «одна плоскость»
  (два нижних ребра на одной линии), чёт-нечёт делал из наложения дыру —
  теперь шов = интерьеры по разные стороны + предупреждение о перекрытии;
  (3) проёмы чёт-нечётом: окно за контуром стены давало облицовку СНАРУЖИ,
  наложение окон — облицовку внутри; теперь вычитание; (4) кольца Г-кусков
  округлялись до 0.001 → микроперекрытия при нулевом русте, теперь 6 знаков;
  (5) запрос без gap ронял движок (AttributeError) — чистый отказ. В C# 09.09:
  метка объединённых зон «A+B» не сохранялась (повтор задваивал раскладку),
  блок «Плитка_<тип>» без формата (второй формат брал размер первого).
  РАСХОЖДЕНИЕ: 143 803 (PDF 09.09) не воспроизводится на фикстуре ни
  старым, ни новым движком — 145 333 (118 367 целых); в PDF 23.09 сказано.
  Вопросы Герману — PDF §9: кляммеры/стойки при разбежке (оси через полмодуля
  при 1/2; тип кляммера на оси «стык снизу — поле сверху»), Г-куски с рустом,
  порог 150 + автоподгонка фазы, случайная разбежка, запас/пропил/DWG.
- **09.09d — «Собираем» №19 (Денис) — ВЫЛОЖЕНО в `latest`.** build-trigger
  `059958c` = bd2a528 + патч build.yml (branches main,build-trigger), один
  force-push; `build-bundle` run #92 (id 34402230874) зелёный, все пять
  архивов в `latest` обновлены 2026-09-09T20:41:54Z, неизменяемый снимок
  — релиз `build-92`. AClad.bundle.zip (8,2 МБ) СКАЧАН И ВСКРЫТ из среды
  claude.ai: PackageContents — ATCLAD/ATCLADDIM/ATTILE, ACladPlugin.dll
  56 КБ (штамп 09.09.2026 20:41 — это увидит Герман), clad_engine.exe
  8,3 МБ с модулем tile_pattern. ГРАБЛЯ-4 (403 на releases/download*)
  в claude.ai НЕ действует — ассеты качаются curl'ом без токена.
  **Инструкция Герману — `docs/ATTILE_manual_0909.pdf` (генератор
  `make_attile_0909.py`):** установка (достаточно заменить AClad.bundle,
  если стоит №18), требования к образцу (полилинии/штриховки, тип = слой,
  цвет = цвет слоя, полный прямоугольник NC×NR), диалог с дефолтами,
  слои/блоки вывода, чтение сводки и раскроя, сверка 143 803, четыре
  открытых вопроса (запас %, пропил, граница раппорта, DWG-эталон),
  быстрая проверка. Кнопки ATTILE на панели «Фасады» нет (набирать с
  клавиатуры) — добавить в следующий заход вместе со строкой загрузки
  Plugin.cs (упоминает только ATCLAD). Ждём тест Германа (Э2).
- **09.09b — ОТВЕТЫ ГЕРМАНА ПО ATTILE ПРИМЕНЕНЫ + РАСКРОЙ.** Ответы
  (через Дениса, на вопросы §5 PDF-сопоставления): В1 датум — как у
  GPT (угол габарита, дефолт «Габарит»; «Стена»/«Точка» остались
  выбором); В2 смежные стены Б4–Б7 — НЕ независимы (одна плоскость:
  `merge_touching`, контуры со стыком ≤ 1 мм объединяются в зону с
  общей сеткой, координаты шва сводятся к общему значению); В3
  полоски — поглощать рустами, в раскладке не учитывать
  (`tiny_mode=absorb`, порог 10 мм в диалоге). Вопрос Германа
  «делался ли раскрой, как учтены обрезки и отходы — у Логоса на ~10 %
  больше»: корень найден — считалось «плитка на кусок» (+12 % к
  площади). Введён раскрой `nest_pieces` (полосы полной высоты — по
  ширине FFD, полной ширины — по высоте, пропил 3 мм в диалоге,
  фигурные — плитка на кусок) + отходы отдельной строкой. Итог на
  эталоне 290×82: **143 803 плитки (−9.1 % к прежним 158 230; минимум
  по площади 140 639; отходы раскроя 2.2 %)**, целых 118 627, кусков
  41 342, полосок поглощено 1 952. Сверка с GPT по правилам Германа —
  **до штуки в 14 зонах из 17**; расхождения — дефекты GPT (щель 0.056
  мм в их объединённой B04 резала каждую плитку через шов: −1520
  целых; микроскос рёбер на «ножках» Т3/Т5 давал микронные куски).
  Файлы: `tile_pattern.py` (merge_touching, absorb, nest_pieces,
  datum_wall с сокращением внутренних швов, DEFAULT_DATUM=bbox),
  `clad_engine.py` (проброс kerf/tiny_mode/merge), `TilePatternCommand.cs`
  (дефолт «Габарит», вопросы «одна плоскость?» и «пропил», сводка с
  раскроем), `tools/tile_pattern_dxf.py` (xlsx с раскроем/отходами),
  91 юнит. Открыто Герману: запас на бой/брак (%), пропил (3 мм?),
  граница раппорта, — `docs/TILE_PATTERN.md`. Push из Cowork НЕ
  ПРОШЁЛ (грабля ниже); **09.09c — оба коммита (e60a1e3, bd2a528)
  залиты из claude.ai через `git am` патчей из проекта, check
  #34401560809 зелёный (TilePatternCommand.cs компилируется),
  91/12/58 юнитов зелёные.** Форма URL для push из claude.ai:
  `https://x-access-token:<PAT>@github.com/…` — голый `<PAT>@` без
  пароля даёт «could not read Password» (нет tty). Из двух
  присланных PAT первый — 401 (невалиден), сработал второй;
  засвеченные в чате токены — отозвать.
  ⚠ ГРАБЛЯ СРЕДЫ (09.09): git-прокси Cowork теперь ОТБРАСЫВАЕТ
  токен из URL и подставляет свои учётные данные только для репо из
  источников сессии («not in this session's authorized repository
  set»); клон публичного репо проходит без токена, push — 403. В
  июле-августе токен-в-URL работал (другая инфраструктура). Лечение —
  добавить `zinchenko-denis/AutoCAD` в GitHub-источники сессии/
  проекта Cowork; повторные push при 403 README прокси запрещает.
- **09.09 — НОВАЯ КОМАНДА ATTILE: мелкоштучная раскладка по образцу
  (плитка/кирпич с раскраской). Не путать с ATCLAD (кассеты) —
  отдельный алгоритм, общий движок/бандл.** Задание Германа 09.09:
  бетонная плитка 290×82, руст 7, шахматка со сдвигом рядов на
  полплиты, цвета по типовой схеме 6×10 (терракот T1–T3 / беж T4–T6).
  Денис принёс два готовых расчёта (свой прототип на shapely +
  пакет GPT-6 Astra) — оба сверены (совпали до штуки на геометрии,
  разошлись на 3 сознательных допущениях; сравнение — в claude.ai-
  проекте). Из сессии сделан ПРОДУКТ:
  - `engine/tile_pattern.py` — движок на ЧИСТОМ stdlib (shapely
    прототипа переписан под PyInstaller): slab-декомпозиция зоны,
    компоненты связности для Г-кусков, раппорт из образца, датум
    «стена/габарит/точка», порог полосок, заготовки с половинками.
  - `engine/clad_engine.py` — `op="tile_pattern"` рядом с cladding.
  - `engine/test_tile_pattern.py` — 68 юнитов (ручной счёт); в CI.
  - `src/ACladPlugin/TilePatternCommand.cs` — команда **ATTILE**
    (образец → раппорт → зоны → крашеные плитки по слоям типов;
    целые = блоки `Плитка_<тип>` под ATSPEC, подрезка/полоски —
    свои слои; метка ATTILE, перегенерация). Переиспользует
    internal-хелперы CladCommand (CallEngine/StoreData/ReadData/…).
  - `bundle/…/PackageContents.xml` — команда ATTILE зарегистрирована.
  - `tools/tile_pattern_dxf.py` + `tools/testdata/tiles290/` —
    обкатка на живом эталоне без AutoCAD: DXF→движок→DXF+xlsx.
    Прогон: 118 489 целых, 3346 м², рендер верный.
  - `docs/TILE_PATTERN.md` — модель/алгоритм/вопросы Герману.
  **Сверка с прототипом:** совпадение по всем зонам, кроме Т3/Т5
  («ножки»), где расходится ТОЛЬКО выбор датума (прототип случайно
  падал на min-Y из-за скошенного ребра; stdlib снапит и берёт
  доминирующее ребро стены — принципиальнее). Δ = +130 плиток из
  118k = 0.11%, весь в этих двух зонах.
  **НЕ проверено вживую в AutoCAD** (C# компилируется только на CI):
  ATTILE ждёт сборку и тест Германа. Датум по умолчанию — «Стена»;
  открытые вопросы В1–В5 — `docs/TILE_PATTERN.md` §Открытые вопросы.
  **ГРАБЛЯ (подтверждена на этой задаче):** stdlib-ортогональность
  контуров из АР — недостаточно `_ortho_snap`, нужна кластеризация
  координат (остаточная невязка 0.01 мм роняла зону целиком). В
  tile_pattern сделано (`_quantize`); если в cladding_plan всплывёт
  та же беда на реальном контуре — перенести оттуда.
- **07.08b — наименование облицовки ATCLAD выбором из списка слоёв.**
  Просьба Германа 07.08 («везде, где программа просит указать слой, —
  выбор из списка существующих»): запрос «Наименование облицовки»
  переведён из командной строки в форму `LayerPickForm` (комбо
  DropDown: слои чертежа + свободный ввод; дефолт прежний — из метки/
  слоя образца). Сопутствующее в AFrame: хлыстовый режим, типы
  кляммеров, ATDEDUP — см. `AFrame/docs/NEXT.md` §07.08b.
- **04.08g — ATCLADDIM: область и выбор слоя (Р-1/Р-2).** Команда
  сначала просит выбрать область рамкой (фильтр INSERT) — ряд/столбец
  ищется только среди выбранных вхождений; Enter без выбора оставляет
  прежнее поведение по всему чертежу. Слой размеров выбирается в форме
  `DimForm` (WinForms, собрана кодом) из списка существующих слоёв со
  свободным вводом, дефолт `_РАЗМЕРЫ_ОБЛ`. В csproj добавлены рефы
  System.Windows.Forms и System.Drawing.
- **04.08e — ОРТОГОНАЛЬНОСТЬ КОНТУРА (жалоба Германа по сборке №16:
  «сложные области не раскладываются, программа очень чувствительна к
  ортогональности сторон»).** Корень: `_is_ortho` требовал точности
  1e-6 мм — отклонение в СОТУЮ ДОЛЮ миллиметра роняло раскладку всего
  контура, и нота молчала о причине. Проверено: сложные ортогональные
  формы работали и раньше (Г-образная 24 камня, ступенчатая 35) —
  падала именно неточность рёбер, обычная для контура из АР.
  `_ortho_snap`: почти ортогональные рёбра выпрямляются в пределах
  `ortho_tol` (дефолт 5 мм, поле запроса), два прохода — второй ловит
  рёбра, ставшие ортогональными после сдвига соседа. Реально наклонные
  (скос, арка) отклоняются, но нота называет отклонение и допуск.
  Юниты C35–C38 — 58 зелёных, аудит 124 чист.
  **Осталось из фидбэка:** ATCLADDIM ставит размеры по всему чертежу
  вместо заданной области (Р-1) и не даёт выбрать слой из списка
  существующих (Р-2) — следующий заход, нужна форма.
- **04.08c — БАГ ATCLADDIM «панель <1250 — размер ерунда» (Денис):
  причина найдена в коде, фикс + встроенная диагностика.**
  `ATCLADDIM` брал габарит камня сырым `br.GeometricExtents`, а он
  возвращает ВСЁ содержимое вставки, не только контур: тексты
  атрибутов (с 01.08b ATCLAD дописывает в кассету МАРКИРОВКА/ЗАХВАТКА),
  заливки-видимости, вспомогательную графику. Разбор универсального
  блока `testdata/dxf/facades/blocks/Universalny_blok_oblitsovki_kasseta2.dwg`
  (aspose напрямую): контур камня — ОДНА LWPOLYLINE, в базовом
  определении 1240×1377, в растянутом представлении `*U4` — 1150×1205,
  то есть контур растягивается корректно; рядом лежат ATTDEF
  МАРКИРОВКА (h=130, поз. 597.9/616.5) + ЗАХВАТКА (h=50, поз. 0/0) и
  12 объектов на слоях «цвет 2..12» (aspose их не разбирает). Габарит,
  залипший на ~1240, объясняет ровно наблюдаемый порог: пока камень
  КРУПНЕЕ залипшего — размер верный, как только мельче — размер
  показывает залипшую величину; попутно полоса отбора ряда (строится
  по габариту кликнутого) начинает цеплять камни соседних рядов.
  **Фикс:** `CellExtents` — габарит только по КРИВЫМ (`Curve`) текущего
  (уже растянутого) определения, трансформированным `BlockTransform`;
  тексты/атрибуты/размеры/штриховки/солиды в габарит камня не входят.
  Фолбэк — сырой extents, если кривых в блоке нет. Белый список вместо
  чёрного (в ATableSpec.AddGab та же грабля ловилась чёрным). Заменены
  оба места: габарит кликнутого (полоса ряда) и габарит каждого камня.
  **Диагностика:** ATCLADDIM печатает «камень по контуру W x H; весь
  блок (с текстами/заливками) W2 x H2» — файл Дениса в чат не влезает,
  поэтому точную причину назовёт сам AutoCAD (разница = виновник).
  ATFRAMEDIM проверен — там координаты берутся из `Position`, той же
  болезни нет. `mcs --parse` чист; полная компиляция — CI.
- **24.07b — ЭТАП 2 ВРЕМЕННО ПРИНЯТ (Денис); другие типы/алгоритмы
  раскладки — позже, шаблонами. СТАРТ ЭТАПА 3 (подсистема: кронштейны
  + направляющие): концепт написан и закоммичен —
  `Facades/docs/STAGE3_FRAME.md` (модуль AFrame отдельным бандлом,
  справочник систем как данные, мост из метки ATCLAD → оси рустов,
  диалог ATFRAME, модель op=frame, В13–В20 Герману, этапность Э1–Э4).
  Материалы 18.07 в testdata ПОДТВЕРЖДЕНЫ и изучены: АТР Вектор-1/5,
  NordFOX, ТР 161-05, СП нагрузки, проект Бугры-2018 (Вектор: 608 по
  каждому русту, кронштейны 1200/800), боевой Ленпроспект-2026 (КАФТ
  Standart: NL/NM/S 210, Г-40×40 хлысты 6000, анкеры 800, направляющие
  «2 на плиту у проёмов / 1 на стык в глухих», терморазрыв 10) +
  конспекты kb_private и kb/. Ключевое решение: правила расстановки —
  атрибуты СИСТЕМЫ (дельта КАФТ↔Вектор), расчёт несущей способности —
  этап 4 (методики закинет Денис). В метку ATCLAD при следующей правке
  добавить joints_x[]/rows_y[] (мост к AFrame). DXF листов 7–10
  Ленпроспекта для D-эталона — так и не присланы (запрошены 18.07).**
- **24.07a — В12 ЗАКРЫТ (ответ Дениса за Германа) + «Собираем» №5.**
  Мелкие куски ПО ВЫСОТЕ (под/над окном, ступени/парапет) теперь
  СТАВЯТСЯ — «отрабатываем принцип, кусок ставим; полоску позже
  прикроет отлив»; min_cut остался порогом только по ШИРИНЕ (В4).
  Подтверждено: подрезка приходит к верху окна СНИЗУ (зоны слева/
  справа). Notes информативные («мелкая подрезка по высоте N мм у
  проёма», «мелкий ряд ...»). Тесты 49+12 зелёные (C30c/d
  переписаны). Инструкция Герману по изменениям — текстом в чат
  Дениса. Открыт П1-перф; эталон DWG — ждём от Германа.
- **23.07f — ФИДБЭК ГЕРМАНА №2 (первый ЖИВОЙ прогон ATCLAD, 6 скринов
  + разбор Дениса) — ДВА КОРНЯ НАЙДЕНЫ И ЗАКРЫТЫ В ДВИЖКЕ (C# не
  тронут).** Вертикальную раскладку Герман подтвердил («разбил
  правильно»). Дефекты: **(1) сдвоенные низкие плитки СБОКУ окон +
  белая пустая полоса через весь фасад на уровне низа окон** (в
  командной строке «высотой 106 < min_cut» ×2). Корень: strip_bands
  резала полосу ряда горизонтальными рёбрами ВСЕХ полигонов — низ/
  верх окна дробил и боковые столбцы, а подполоса
  106<150 выпадала ВО ВСЮ ширину. Фикс: подполосы только по ВНЕШНЕМУ
  контуру; проёмы вычитаются по Y (bbox ± gh) только из камней,
  перекрытых проёмом по X; пролёты внутри проёмов — dead_x, без
  ложных notes; ниша в середине ряда даёт камень под И над собой.
  Боковые столбцы теперь ЦЕЛЫЕ. C17 (кодировал старое поведение как
  «фичу П4») переписан. **(2) «раздвинутый руст» у граней окон,
  замер Дениса 4.0000 мм**: клик верт. руста в угол окна создавал
  ось-шов [грань±4] ПОВЕРХ авто-шва [грань, грань+8] → вырез 12 мм и
  несоосность кромок поясов на gv/2. Фикс: юзер-vjoint,
  пересекающийся с авто-швом грани проёма, схлопывается (авто-шов
  главнее); vjoint в простенке — осевой, как был. Тесты: 40→**48
  проверок** (C29–C33 — модель фасада Германа 3000×3000, окно
  600×1400, русты 8: боковые целые, под окном 600+384, остаток 16 —
  note, дедуп vjoint=идентичный план, ниша) **+ 12 CLI зелёные**.
  ОТКРЫТО: **В12** — остаток по высоте под/над окном < min_cut
  (сейчас note+пусто; ставить мелкую? перераскладывать пояс?);
  **П1-перф** — 1.5 мин на участок (вставка сотен динблоков с
  атрибутами; оптимизация после приёмки геометрии); DWG-эталона
  ручной раскладки в testdata ПО-ПРЕЖНЕМУ НЕТ (Денис думал, что
  есть — только блок «кассета 2») — запрошен у Германа. Ждёт
  «Собираем».
- **23.07e — «Собираем» №4 (Денис): build-trigger `a8084fc` =
  de3f382 (фикс дубля hjoints + hjoints в метке + try/catch RunCore)
  + ci-патч. Один force-push по протоколу; статус — глазами Дениса.
  Инструкция Герману (установка только AClad + функции фазы 2) —
  текстом в чат Дениса.**
- **23.07d — КРАШ ATCLAD У ГЕРМАНА НАЙДЕН И ЗАКРЫТ (его скрины:
  ArgumentException «Элемент с тем же ключом уже был добавлен»,
  стек Dictionary`2.Insert ← CladCommand.Run).** Стек же доказал,
  что установка #76 у него ВСТАЛА (класс ACladPlugin существует
  только в новом бандле) — вопрос «старой версии» снят. Корень:
  в инициализаторе payload ключ «hjoints» стоял ДВАЖДЫ (кривая
  вставка патча 22.07c) — инициализатор компилируется в два
  Add(«hjoints»,…), второй кидает; краш ДЕТЕРМИНИРОВАННЫЙ, на
  каждом запуске, сразу после Enter на последнем вопросе диалога.
  ⚠ ГРАБЛЯ НОВАЯ: дубль литерального ключа в инициализаторе
  словаря НЕ ловится ни mcs --parse, НИ ПОЛНЫМ КОМПИЛЯТОРОМ CI
  (легальный C#) — только рантайм; после каждого патча payload/meta
  гонять скан дублей (скрипт в журнале 23.07d, добавить в привычку
  рядом с грепом хелперов). Фикс: дубль убран; заодно (а) hjoints
  ДОБАВЛЕН в метку ATCLAD — 22.07c обещал «в payload и метку», в
  метке его не было (пресеты бы потеряли гориз. русты); (б) тело
  команды обёрнуто try/catch → RunCore: рантайм-ошибка теперь
  печатает стек копируемым текстом в командную строку, а не роняет
  AutoCAD краш-окном. mcs --parse OK, аудит хелперов OK, скан
  дублей чист. Движок не менялся (40+12 зелёные с утра). Ждёт
  «Собираем».
- **23.07c — ДИАГНОСТИКА «у Германа старая версия, нет фазы 2».**
  Наша сторона проверена и чиста: манифесты бандлов ок (ProductCode
  РАЗНЫЕ GUID, LoadOnAutoCADStartup, пути DLL), build.yml собирает и
  публикует все 4 бандла, origin/build-trigger = 9a5e136 = актуальный
  код (дифф с веткой — только docs), ATCLAD/BuildStamp в DLL есть.
  ВЕРОЯТНАЯ ПРИЧИНА — ХРОНОЛОГИЯ: обе сборки 22.07 (#74, #75) красные
  → publish НЕ выполнялся → до ночи 23.07 в latest лежал СТАРЫЙ
  AFacades (21.07-3, ещё с ATCLAD ранней ревизии), а AClad.bundle.zip
  ОТСУТСТВОВАЛ (ссылка → 404); инструкция Герману ушла 22.07 — если
  он качал до завершения #76, получил ровно наблюдаемое: старая дата
  «сборка DLL от…» + нет фазы 2. Решение: перекачать ОБА zip (сейчас
  в latest свежие от #76), удалить обе старые папки из
  ApplicationPlugins, контроль — ДВЕ строки «сборка DLL от 23.07…»
  при старте. ГРАБЛЯ-4 РАСШИРЕНА: прокси среды режет
  releases/download/* с 403 в ОБЕИХ формах авторизации (token-URL и
  Basic extraHeader) — ассеты релиза из среды не скачать и не
  вскрыть; состав latest проверяем только хронологией зелёных
  publish-ранов + скринами Дениса (глазами).
- **23.07b — СБОРКА ЗЕЛЁНАЯ (скрин Дениса): build-bundle #76 =
  9a5e136 (фикс SafeStr), check #102/#103 зелёные.** AClad.bundle.zip
  ВПЕРВЫЕ в latest — ссылка второй части у Германа работает; ему
  ставить ОБА бандла по инструкции. Сессия завершена ХЭНДОФФОМ:
  промт переноса ОБНОВЛЁН (Facades/docs/HANDOFF_PROMPT.md, версия
  23.07 — состояние двух модулей, задача «фидбэк Германа по фазе 2»,
  все грабли включая mcs-parse≠компилятор и stale-кэш WebFetch).
  Следующая сессия стартует по нему + токен от Дениса.
- **23.07a — ФИКС КРАСНОЙ СБОРКИ (лог Дениса со скринов): CS0103
  «SafeStr does not exist» ×14 в CladCommand.cs** — при выделении
  модуля перенёс все хелперы, КРОМЕ SafeStr (добавлен). Обе сборки
  22.07 (#74 bfac18f, #75 e0d9add) и check #98–#101 падали только на
  этом — остальной новый API (PromptStringOptions, PromptPointOptions
  .AllowNone, Erase/UpgradeOpen/Highlight) полным компилятором ПРОШЁЛ
  (сборка 21.07-3 зелёная, в красных логах других ошибок нет).
  ГРАБЛЯ в копилку: mcs --parse НЕ ловит CS0103 (незнакомые имена —
  семантика); при переносе кода в новый модуль — грепать ВЫЗОВЫ
  хелперов против ОПРЕДЕЛЕНИЙ (аудит-скрипт в журнале сессии), не
  полагаться на parse. Новый build-trigger от фикса — сборка №3.
- **22.07d — «Собираем» №2 (Денис).** build-trigger `e0d9add` =
  058f6f7 (ТЗ 22.07 полностью, с Г1–Г3); перетёрт bfac18f (без
  Г1–Г3 — если Герман успел поставить утренние бандлы, ему достаточно
  обновить только AClad.bundle). Полная инструкция (установка блока 1
  + блока 2 + что сделано по замечаниям) — текстом в чат Дениса.
- **22.07c — ОТВЕТЫ Г1–Г3 ПОЛУЧЕНЫ И РЕАЛИЗОВАНЫ (все три).**
  Г1: `_seg_from_corner` — подрезка у шва <300 → плитка перед ней в
  половину (w/2), подрезка = rem+w/2 (оба куска ≥300 при 600-м камне,
  дыр у окна не бывает; зеркально для правого угла — шов ровно gv,
  проверено тестом C25). Г2: подтверждено текущее поведение — камень
  над/под узким окном НЕ шире проёма (пролёт между швами, C28);
  «целая шире окна» НЕ нужна — хвост 1.3 закрыт без кода. Г3 (=
  1.4/2.6): `hjoints` — точки горизонтальных рустов, точка = НИЗ
  руста [py, py+gh], действует на ВЕСЬ участок: контур режется на
  ПОЯСА (нижний от origin.y — общий горизонт; каждый следующий от
  py+gh — «панели стандартной высоты»), к русту снизу подрезка
  (C26 — кратный руст без подрезки, C27 — подрезка 390 к русту);
  клик по верху окна = руст над окном = привязка п.1.4. C#: цикл
  точек горизонтальных рустов (после вертикальных), hjoints в
  payload и метку ATCLAD. Движок 40 проверок + 12 CLI; mcs --parse
  чистый. ТЗ 22.07 реализовано ПОЛНОСТЬЮ (1.1–1.6, 2.1–2.6);
  открыты только В10/В11. НЕ выкладывалось — ждёт «Собираем».
- **22.07b — «Собираем» (Денис).** build-trigger `bfac18f` = ae98ef7
  (разделение + ТЗ 22.07) + патч build.yml, один force-push; первая
  сборка бандла AClad.bundle.zip (и AFacades без ATCLAD). Две
  инструкции (установка блока 1 и блока 2) — текстом в чат Дениса.
- **22.07a — МОДУЛЬ СОЗДАН (разделение по просьбе Германа) + движок
  по ТЗ 22.07.** Перенос из Facades: cladding_plan/тесты/CLADDING/
  мануал; новый `clad_engine.py` (op=cladding + своя группировка
  контуров: bbox-центр+центроид пробы, глубина ≤1, дуги — пропуск);
  `CladCommand.cs` самодостаточен (свои Xrecord/CallEngine/LayerName;
  namespace ACladPlugin), диалог по ТЗ 2.1–2.5: макс. размеры камня
  (выставляются и образцу), наименование → слой «Облицовка <тип>»,
  точка старта (Y-горизонт), русты, ЦИКЛ точек вертикальных рустов
  (PromptPointOptions AllowNone — прецедента не было, проверит CI);
  способ [Проемы/Край] УБРАН. Движок: `_seg_centered` (1.2/1.3:
  блок целых по центру, кусок <300 — сдвиг на полплиты; заменил В5),
  `_seg_from_corner` (1.6: от угла целыми, подрезка у шва), vjoints
  (2.5: ось шва по точке, вся высота), mode-дефолт = основной
  (edge — запасной, в диалоге его нет). Facades почищен (op=cladding
  и CladCommand изъяты, 57 юнитов ✓); CI: build.yml собирает 4-й
  бандл AClad.bundle.zip, check.yml компилирует ACladPlugin и гоняет
  тесты AClad. Тесты: 36 (движок, C14/C21/C22 — центрирование и
  сдвиг, C23 — vjoints, C24 — дефолт-режим) + 12 (CLI). ⚠ Установка у
  Германа: заменить AFacades (без ATCLAD) И поставить AClad — старый
  AFacades с ATCLAD + новый AClad дадут ДВЕ команды ATCLAD.
